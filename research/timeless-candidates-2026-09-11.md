# Timeless candidates after Step 62 (2026-09-11)

Research note. Not a decision. Produced by a 26-agent Opus workflow run from the factory session on 2026-09-11 (8 surface readers, 1 merge, 15 refuters in three lenses, 1 writer, 1 completeness critic). Everything below the "Factory session verification" section is agent output, kept verbatim so the evidence trail survives; treat every count, citation, and effort figure in it as reported, not verified, unless this header says otherwise.

## Factory session verification (what the factory session checked by hand)

- Candidate C01 (stale self-counts) is real. Opened on the published product at 5c305ef: bootstrap/ONBOARD.md:59 says "25 convention docs" and :366 "all 28 convention docs" (29 files exist); scaffolding/_preamble.md:44 and scaffolding/SCAFFOLD.md:35 say "13 silent-failure patterns" (18 numbered headings in scaffolding/RED-FLAGS.md); development/DEVELOP.md:20 says "7 Phase 3 silent-failure patterns" (9 numbered headings); META-BATTLE-TESTING.md:14 says "28 conventions". Six stale numbers, all outside validate-framework.sh group 2, which reads only Conventions.md.
- No other candidate's evidence was opened by the factory session. The completeness critic below spot-checked several and found misreadings; its list is the first thing to read before funding any candidate.
- The refute stage returned zero candidates refuted by two or more lenses out of 24. Read that as a weakness of the refuters, not as strength of the candidates: the critic found contradictions between candidates, one refutation resting on a misread line, and counts that three verifiers each got wrong in different ways.
- Effort figures are agent T-shirt sizes translated to days by the writer agent. The factory session did not estimate anything.

## Factory session reading of the result

The 24 candidates cluster into a few real directions, ordered by how much the factory session trusts them:

1. Self-tests for the framework's own claims about itself: part counts, id ranges, cross-references, "a validator checks this" statements, enforcer-to-rule agreement (C01, C05, C06). C01 is verified; the others are plausible and cheap to check.
2. Extend the expiry gate to everything shipped and broaden what it detects (C13, C23, C22, C03, C16): this is Step 62 follow-up 2 with more precision about what the playbooks contain.
3. A home and a verification stamp for every project-chosen setting, plus recorded deferrals in bootstrap (C02, C04, C12, C21).
4. Decouple from one AI host, one code forge, one VCS workflow, and one hook ABI; pin and roll back installed revisions (C07, C08, C11, C15).
5. Phase model and playbook structure: named steps, dependency-derived order, re-entry, backend and security rules stated as obligations plus recorded method (C18, C19, C20, C14, C24).
6. Multi-actor authority model (C09, C10): furthest from today's use; park until a second operator exists.

Not in the sweep, per the critic: the factory's own research/ folder and planning/BACKEND-AUDIT.md still carry the content Step 62 deleted from the product; the factory root README is stale; no automated trigger runs any gate (no CI in either repo); the gates assume BSD or GNU tools inconsistently; the product has no license file.

## Founder summary as written by the writer agent (verbatim)

Archetype track, candidates found after Step 62:
1. Make the framework prove its claims about itself instead of asserting them: its own part counts, its internal cross-references, the promises that a check exists, and the contract behind the project health view. Six of its own numbers are reportedly wrong today and four defenses name checks nobody ever wrote. Roughly 13 to 22 days.
2. Give every project-chosen setting a real place to live, a "last verified on" date, and a written record of unanswered compliance questions. Today some rules tell a project to record a decision in a place that has no slot for it, and a deferred compliance answer is read downstream as a clean "no". Roughly 9 to 15 days.
3. Make the automatic checks read what each project declared rather than guessing, and let a check say "does not apply here" instead of quietly passing. Roughly 15 to 25 days.
4. Stop welding the framework to one AI tool and one code-hosting account: one place names the host and the download source, everything else reads it, and a project can pin or roll back what it installs. Roughly 12 to 20 days.
5. Write down who approves what, and stop two workers silently overwriting the same shared list; state the save-and-review rules as properties so a project on different tooling can still obey them. Roughly 9 to 15 days.
6. Give the build-order steps permanent names instead of positions, derive the order from real dependencies, and let a project re-enter a phase when it later adds a platform. Roughly 9 to 15 days.
7. Point the expiry checker at everything shipped, not a fixed list of files and a fixed list of vendor names, so a product name invented years from now is caught rather than passing silently. Roughly 6 to 10 days.
8. Turn the remaining era-bound numbers and the undated beliefs about how AI behaves into project-set values and dated observations, so a rule never rests on a reason that has quietly become false. Roughly 12 to 20 days.
9. Restate the server-side and security rules as the obligation plus the method the project recorded, so a project on different infrastructure can obey them instead of ignoring them. Roughly 9 to 15 days.
10. Strip one programming ecosystem's file names and code words, one machine's folder paths, a private project name, and edit-history storytelling out of the shipped rules. Roughly 12 to 20 days.
None were refuted: all 24 findings survived three independent reviews each, and the ten lines above bundle those 24 in the order I would do them.
One finding (era-bound numbers) drew a single dissenting reviewer who called most of it overreach rather than wrong; every day figure and every count above is a reviewing agent's estimate, not something this session verified.

---

# Timeless candidates after Step 62 (2026-09-11)

## Provenance and status of every claim in this document

Read this section before citing anything below.

- **Everything here is agent-reported.** Every file:line citation, quotation, count, validator-run result, verdict and effort size below was produced by the sweep's reading agents and by three independent verifier agents per candidate. The factory session that assembled this document read only `planning/STEP-62-TIMELESS-CONVENTIONS.md` in full and confirmed the product revision; it re-verified **no** evidence line, **no** count, **no** validator run and **no** effort figure. Treat every statement as "reported by an agent, not verified by the factory session" unless it is explicitly marked otherwise.
- **Two facts are factory-session observed:** the factory repository is at `edf7b11` and the product checkout `dist/` is at `5c305ef`; `dist/scripts/` contains `pulse-inspect.sh`, `test-entrypoints.py`, `timeless-allowlist.txt`, `timeless-terms.txt`, `validate-develop.sh`, `validate-framework.sh`, `validate-maintain.sh`, `validate-migration.sh`, `validate-scaffold.sh`, `validate-timeless.sh` and **no** `validate-bootstrap.sh`.
- **Paths.** All paths are relative to `/Users/d3r3nic/Development2/ai-dev-framework`; `dist/` is the published product, `planning/` the factory source of truth.
- **Nothing was modified.** The sweep was read-only; reported validator runs were made with explicit file arguments, which write nothing.
- **Effort notation.** Agents reported T-shirt sizes. The day ranges in this document are the factory session's fixed translation of those reported sizes, not an independent estimate: **S = 1-2 days, M = 3-5 days, L = 6-10 days.** Where the three verifiers disagreed, the consensus is the majority size and the spread is shown.
- **Step 62 scope, for the avoidance of re-proposal.** Step 62 shipped `dist/scripts/validate-timeless.sh` (classes A-F), the term list and allowlist, remediation of 29 universal plus 7 backend conventions, the enforcers, README and `templates/*.md`, and recorded four follow-ups: (1) shape-agnostic core with expirable profiles, (2) phase playbooks and remaining docs into scope, (3) scripts and UI assets not scanned, (4) updater no-op log entries. Candidates that touch those follow-ups are flagged per candidate; none of the 24 is a restatement of what Step 62 already did.

## Field key

Each survivor carries: **Kind** (expirable-content / missing-mechanism / structural-assumption), **Value** (1-5, sweep-assigned), **Sources** (which reading surface found it), **Evidence**, **Why it expires**, **Timeless form**, **Intent risk** (what force would be lost if the rewrite were done carelessly — load-bearing, not decoration), **Verifier notes** (three independent adversarial reviews, distilled; corrections and required amendments kept), **Adjusted effort**.

---

## C01 — Framework self-counts and id ranges are hand-typed prose in both repos; five are already wrong

- **Kind:** expirable-content · **Value:** 5 · **Sources:** bootstrap, scaffolding, development, factory-design
- **Adjusted effort:** reported S / M / S → **S (1-2 days)**, consensus S

**Evidence (agent-reported)**

- `dist/bootstrap/ONBOARD.md:59` "# 25 convention docs" and `:366` "Do NOT read all 28 convention docs upfront" — against `ls conventions/*.md` = 29.
- `dist/scaffolding/_preamble.md:44` "13 known silent-failure patterns", same claim `dist/scaffolding/SCAFFOLD.md:35` — against `grep -c '^## [0-9]' scaffolding/RED-FLAGS.md` = 18.
- `dist/development/DEVELOP.md:20` "RED-FLAGS.md — 7 Phase 3 silent-failure patterns" — against 9; `dist/META-BATTLE-TESTING.md:14` "28 conventions"; `dist/README.md:74` "01-28.md".
- `dist/scripts/validate-framework.sh:66` reads the count claim only from `Conventions.md` (`all [0-9]+ convention`), so every other file's number is unguarded.
- `planning/ARCHITECTURE.md:50` "19 enforcement rules", `:59` "all 25 conventions", `:65` "One file per convention (23 files)"; `planning/CONVENTIONS-CATALOG.md:3` "= 25 total", with shipped #23-#27 absent from the catalog that factory `CLAUDE.md:21` makes a precondition for writing convention content.

**Why it expires.** A count of the framework's own parts is false the moment a part is added, and drift is monotonic: stale numbers coexist today across playbooks, storefront, meta doc and the planning source of truth with nothing failing. Step 62 corrected 28→29 by hand and recorded it as "not a validator finding" (`STEP-62:62`).

**Timeless form.** No shipped or planning doc states a count or id range of framework artifacts; cross-references name the artifact ("the Phase 3 silent-failure catalogue", "every numbered convention doc") and keep the orientation signal as a quality ("short enough to read whole"), not a quantity. Generalize `validate-framework.sh` group 2 from "the claim in Conventions.md" to any `N conventions|patterns|playbooks|NN-NN.md` claim in any managed file, derived from `find`. Add id-set parity both ways between `dist/conventions/` and `planning/CONVENTIONS-CATALOG.md` so a convention shipped without a catalog entry fails the factory check, mechanizing "map before territory". Where a count helps a reader it lives in a generated block.

**Intent risk.** The numbers carry orientation ("it is short, read it all") and the catalog count encodes the map-before-territory gate; naming the quality keeps the nudge and derived parity keeps the gate.

**Verifier notes**

- **V1 held.** Verified line by line; counts six wrong numbers, not five. Two caveats that do not refute: `README.md:74` "01-28.md" is currently correct (line 72 lists `00-reusability.md` separately, so 00 + 01-28 = 29), and strict catalog parity collides with factory `CLAUDE.md:31` "do not overwrite history - append" plus the catalog's A1/A2 addons with no shipped files, so parity needs an explicit mapping rule rather than a raw set diff. Effort S.
- **V2 held.** Same verification; adds that a blanket ban on counts would retire the one count `validate-framework.sh` already keeps honest, which the proposal's "generated block" escape covers. Raised effort to **M** because making parity pass requires backfilling five real catalog entries (#23-#27) in the existing format plus correcting three `ARCHITECTURE.md` numbers.
- **V3 held.** Confirms the catalog heading set runs #0-#22 then jumps to #28, and that `STEP-62:62` records the 28→29 fix as hand-done, so this is not a re-proposal. Classes the `README.md` item as an instance of the drift class rather than a present error. Effort S.

---

## C02 — Dials and recorded decisions have no registry: delegations point at slots that do not exist and scripts hold the numbers as literals

- **Kind:** missing-mechanism · **Value:** 5 · **Sources:** development, scaffolding, conventions-a, backend, templates-scripts, factory-design
- **Adjusted effort:** reported M / M / M → **M (3-5 days)**, unanimous

**Evidence (agent-reported)**

- `dist/development/DEVELOP.md:88` "pick one at scaffold time and document it in References.md" (test isolation) — `grep -ci isolation` on `templates/references-frontend.md` and `references-backend.md` = 0.
- `dist/development/MAINTAIN.md:69` "Threshold is project-specific — 180 days is a reasonable default" vs `dist/scripts/validate-maintain.sh:149` `THRESHOLD_DAYS=180` hard-coded, with no project value read anywhere: the project-specific threshold is unsettable.
- `dist/backend/conventions/B3-middleware.md:48`, `B5:47`, `B7:48` "Document the ... in References.md" — `templates/references-backend.md` has no middleware, observability, queue or cache section (`grep -niE 'middleware|queue|cache|correlation|observab|dead letter'` returns nothing).
- `dist/templates/references-frontend.md:65` "File-size limit: [lines per file ... (#1, #17)]" — the dial-to-rule link is a hand-typed `(#N)` tag inside a bracketed placeholder; `conventions/01:20` and `04:22` delegate by prose with no id.
- `planning/STEP-62-TIMELESS-CONVENTIONS.md:145` "six rewritten rules delegated a number to References.md without the templates carrying a slot" — found by a human auditor, closed by hand-adding seven slots (`:146`).

**Why it expires.** Not time-decay but rot-by-growth. "Make it a project-set dial" is now the framework's main answer to era-bound specifics, so the plumbing is load-bearing: every future de-dated rule adds a delegation whose destination is added by hand or not at all, and an agent that cannot find the slot invents a location or re-uses the convention's illustrative number. The same defect appears in scripts that fail on a literal the project is told it owns.

**Timeless form.** One registry (`scripts/dials.md`): id, what it governs, owning convention or rule, how to choose the value, which template slots must carry it, no default; two kinds, **dial** (a value enforcers lint against) and **record** (a note that must exist). References template slots are generated from it so no shape's template can omit one, and each convention declares "Dials: &lt;ids&gt;" under its heading. Three checks: prose saying "record/document it in References.md" or "the project sets" must resolve to a declared id with a slot in every applicable template; the inverse (an orphan slot no rule delegates to) fails; project-side gates fail on a slot still holding a bracketed placeholder. A convention must not state a number for its own declared dial. Scripts read the project's value and print which source they used instead of holding a literal. Backend templates gain a section per backend convention keyed by its id, each explicitly conditional.

**Intent risk.** A slot list degrades into box-ticking ("Cache: standard") and a registry becomes a second place to forget. Force is kept by making the convention text trigger the check, asking the slot for the decision the rule needs to be enforceable (the order, the header name, the number) with a one-line justification for any loose value, and testing for a filled value rather than a present heading.

**Verifier notes**

- **V1 held.** Every delegation-without-destination reproduced; stresses that `MAINTAIN.md:69` and `validate-maintain.sh:149` contradict each other today. Proposed form is tool- and vendor-neutral. Effort M.
- **V2 held.** Confirms `references-backend.md`'s section list (Git, Structure/Types, Handler, Errors, API/Contract, Auth, Database, Validation, File Storage, Testing, CI/CD) has no destination for B3/B5/B7. Notes Step 62 closed the class by hand for seven frontend dials only, so the backend records and the isolation dial remain open and this is not a re-proposal. Effort M.
- **V3 held.** Counts roughly 20 delegation phrases and 33 convention files referencing References.md, making it load-bearing; fit is framework-layer because both the delegations and the slot templates are framework-shipped; the three checks are grep-and-resolve and script-reads-recorded-value needs only the parsing the inspector already does. Effort M.

---

## C03 — Era-bound policy and capability numbers sit outside class D's lines-and-minutes window

- **Kind:** expirable-content · **Value:** 5 · **Sources:** bootstrap, scaffolding, development, conventions-a, conventions-b, backend, factory-design
- **Adjusted effort:** reported S (by the refuting verifier, for a reduced residue) / L / L → **L (6-10 days)**, majority L
- **Dissent on record:** one of three verifiers refuted this candidate. See Verifier notes and the refuted-strand list.

**Evidence (agent-reported)**

- `dist/conventions/06-styling.md:29` "minimum 44x44 points", `:16` "typically 4px: 4, 8, 12 ..."; `conventions/14-accessibility.md:57` "Target WCAG 2.2 Level AA"; `conventions/13-performance.md:39` "initial bundle is 2MB".
- `dist/backend/CLAUDE.md:16` "over 2 seconds in a request handler" vs `backend/conventions/B5-background-jobs.md:18` "over 1-2 seconds" — the same prohibition with two numbers; `B2:25` "Default page size 20, maximum 100"; `B1:46` "one pool of 20 connections"; `B4:30` "under 1 second".
- `dist/development/MAINTAIN.md:9` "after every N commits (typically 50-100), OR every 2-4 calendar weeks", `:67` "Age >= 2x threshold", `:84` "any theme appearing in 3+ reviews".
- `dist/bootstrap/ONBOARD.md:273` "covers 80%+ of what the user needs", `:289` "even 50%"; `bootstrap/RED-FLAGS.md:77` "re-asked the same core question 3 times"; `EXISTING-PROJECT.md:144` "dated more than a year ago"; `conventions/17-context.md:14` "after two failed corrections", `:21` "5 unrelated tasks".
- `planning/CONVENTIONS-CATALOG.md:356` "Small focused files (&lt;300 lines)" passes class D because no limit word precedes it; `scaffolding/SCAFFOLD-BACKEND.md:178` "depth 10", `:142` "24h is typical", `:217` "typically 3".
- Class D in `validate-timeless.sh:161` matches only limit-word + "N lines" forms and minute cadences, so bare-operator and spelled-numeral forms evade it.

**Why it expires.** These are the species Step 62 converted for file sizes and compaction cadences, in every unit it did not cover: physical dimensions, byte budgets, page sizes, TTLs, pool sizes, retry counts, response budgets, corrections, tasks per session, commits, weeks, days, reviews, patches, coverage percentages and standard-version pins. They are calibrated to one era's devices, networks, runtimes, guidelines and human commit pace, and an agent reads a number in a Rules or enforcer line as law.

**Timeless form.** Widen class D from two nouns to a class of capacity and policy thresholds, matched by unit and comparison form including bare operators and spelled numerals, scoped to Rules lines, enforcer lines and playbook gate lines so illustrative Wrong-vs-Right numbers stay legal. Each number becomes a registered dial (C02) whose entry carries the reasoning that picks it, and the rule keeps its direction: platform wins unless it genuinely cannot; long work always leaves the request path; collections are always bounded; retries are always capped and end in a dead-letter path; accessibility targets the current standard at the level the project's regime requires.

**Intent risk.** The numbers do the enforcing: 44x44 can be linted and "adequate" cannot; "WCAG 2.2 AA" is the sentence an auditor needs; "80%+" is what stops an agent rationalizing a custom build; 180 days is what makes escalation bite. So the form is a **required recorded value**, not an abstraction: obligation unconditional, only the threshold the project's, slot non-optional, enforcer lints the recorded number. A dial with an empty slot becomes a never.

**Verifier notes**

- **V1 REFUTED.** Lines exist, but the evidence does not show the claimed class and the proposal fails its own lens: (a) "44x44 points" is density-independent and standard-derived and "WCAG 2.2 Level AA" is an open-standard pin that `STEP-62:33` explicitly holds is not a tool choice, so stripping standard-version pins reverses a recorded decision and removes the auditor's sentence; (b) several cited numbers are already hedged dials (`MAINTAIN.md:9` "project-specific threshold, typically", `:69` "a reasonable default", `SCAFFOLD-BACKEND.md:142` "adjust to project retry windows", `:217` "typically 3"); (c) the scope gate is undefinable and self-contradicted, since the candidate promises Wrong-vs-Right numbers stay legal then cites `13-performance.md:39` (a Wrong-vs-Right line) and `CONVENTIONS-CATALOG.md:356` (factory-side, outside the validator's dist-only scope); (d) the mechanism trades a false negative for mass false positives — `STEP-62:51` already records class D leaning to false positives, and matching bare operators, spelled numerals and ~20 unit families would fire on HTTP status codes (`B2:26`), backoff ladders (`B5:20`), the RED method (`B4:31`), the 200KB right-example, and judgment heuristics like "5 unrelated tasks" / "two failed corrections". Residue worth keeping: the `backend/CLAUDE.md` 2s vs `B5` 1-2s contradiction plus a handful of true capacity budgets (bundle byte budget, pool size, GraphQL depth limit) into C02's registry — **S**, not a widened validator class.
- **V2 held.** Verified every number and confirmed class D's regex cannot see bare-operator forms, so the widening extends rather than repeats Step 62. Two weaknesses short of refutation: `13-performance.md:39` sits under "Wrong vs Right" which the candidate's own scope rule exempts, and `CONVENTIONS-CATALOG.md:356` passes mainly because planning/ is out of scope. Survives the intent lens only because the obligation stays unconditional, the slot non-optional, the enforcer lints the recorded value and an empty slot denies. Residual loss not fully answered: a researched default like "page size max 100" is framework value and a project agent may choose worse, so each registry entry must carry real selection reasoning. Raised to **L**: ~20 rule lines, a multi-unit pattern family with high false-positive surface (Step 62 needed two audit rounds for two units), and it cannot land before C02.
- **V3 held.** Core instances sit in binding positions (Rules bullets and audit gate lines), and `B5:18` vs `backend/CLAUDE.md:16` is the drift signature of a number masquerading as a principle. Qualifies that about four cited items fall outside the candidate's declared scope (`13-performance.md:39`, `B1:46`, `14-accessibility.md:57` inside the lawful Research Notes zone, `CONVENTIONS-CATALOG.md:356` factory-side) and that the remediation surface is ~15 unit families across 36 conventions, both enforcers, four References templates and playbook gate lines not yet in scope. Raised to **L**.

---

## C04 — Recorded choices carry no verification stamp and nothing ever re-checks them

- **Kind:** missing-mechanism · **Value:** 5 · **Sources:** bootstrap, conventions-a, conventions-b, templates-scripts, backend
- **Adjusted effort:** reported M / M / M → **M (3-5 days)**, unanimous

**Evidence (agent-reported)**

- `dist/conventions/00-reusability.md:62` "Dated notes: anything named in this section is an example from the time of writing and expires. Verify current options at bootstrap." — byte-identical and undated across the whole surface (V1 reports 38 files carrying one unique string; V2 reports 29 universal + 7 backend + 2 templates; V3 reports 36 conventions plus templates. The candidate's "all 15 files" undercounts).
- `dist/templates/references-frontend.md:16-24` the whole Tech Stack block undated ("- Framework: [UI framework chosen at bootstrap]"), while `templates/references-mobile.md:178` ("verify current values at bootstrap and record the date checked") and `references-platform.md:40` ("Include the date of your pricing check here") do ask for the date — two templates solved it by hand.
- `dist/templates/references-platform.md:122` "California CMIA requires 7 years" and `:125` "HIPAA 60 days" stated flat, no source, no date.
- `dist/bootstrap/ONBOARD.md:383` "If not, use your best knowledge and note what should be verified" — the note has no required place to land; `dist/development/FRESHNESS.md:11` "Every action-dependent fact needs its authoritative source, observed revision, observation time ... and an explicit invalidation or age policy".
- `dist/CLAUDE.md:11` "Read References.md and feature-tree.md (always — these are project context)" — every session is told to trust the file with no freshness signal. `ls scripts/` shows no `check-freshness.sh`.

**Why it expires.** The whole timeless bet is that specifics live in References.md, and References.md has no notion of when a specific was last true. A project bootstrapped by a model with two-year-stale knowledge records stale vendors and its own source of truth is then wrong while every convention looks clean; five years on, an undated choice reads exactly like one verified this morning. The framework states the opposite doctrine for development-phase facts, so bootstrap and the project artifact are the phases exempted from it.

**Timeless form.** Give every researched line a provenance tail in the bullet shape the inspector already parses: `Auth: <choice> | verified <date> | source <command run or page read> | review <trigger>`, or the literal token `UNVERIFIED`. Framework side, the dated notice gains "As of: YYYY-MM" and class F requires it. Project side, `scripts/check-freshness.sh` reads References.md, reports missing / present-unverified / verified-at, is called from `validate-maintain.sh` and `update.sh`, and lists every stamp past the project's review-interval dial; the bootstrap gate refuses to finish while a researched line is unstamped, `UNVERIFIED` lawful only when logged as an open pre-production gate. When an update changes a convention's text, `update.sh` lists the References decisions that convention owns as needing re-verification. Any placeholder whose answer is a number someone else controls (fees, retention, notification deadlines, review times) requires the verify marker the store-fee line already has.

**Intent risk.** A stamp nothing reads becomes ceremony, and a re-verify prompt invites churn that swaps a working library for a fashionable one. Bind the stamp to evidence, make "still current" with a new date a pass so the output is a refreshed date or an explicit migration note rather than an upgrade, make age a warning and absence the failure, and keep the honest escape hatch (`UNVERIFIED` plus a logged gate) cheaper than a lie.

**Verifier notes**

- **V1 held** — calls it the strongest timelessness claim of its batch. Checked the implementation risk: `scripts/pulse-inspect.sh:78-80` splits each Tech Stack bullet on the first colon only, so a `| verified ... | source ... | review ...` tail parses without breaking the inspector. Two inaccuracies that do not change the verdict: the "15 files" count understates 38, and `FRESHNESS.md` is general task-state doctrine rather than a development-phase-facts doc. Effort M, with the `update.sh` convention-to-decision list wanting C02's registry for honest ownership.
- **V2 held.** Same core verification; notes the proposal adds obligation only, that absence fails / age warns / "still current, new date" passes so the re-check cannot read as an upgrade mandate, and flags the same C02 dependency. Effort M.
- **V3 held.** Confirms fit is framework-layer (stamp shape is a convention, templates and the readers are framework-shipped) and implementable with bash plus `date` and the existing bullet parser. Effort M.

---

## C05 — Defense lines claim validator coverage the validators do not have, and nothing ties prose to checks

- **Kind:** missing-mechanism · **Value:** 5 · **Sources:** scaffolding, development
- **Adjusted effort:** reported M / M / M → **M (3-5 days)**, unanimous

**Evidence (agent-reported)**

- `dist/scaffolding/RED-FLAGS.md:45` "Both named in validate-scaffold.sh expected-paths check" — the script has no expected-paths check and no rate-limit or idempotency check; `:93` "can grep for obviously-stale version strings" — no such grep exists.
- `dist/development/RED-FLAGS.md:11` "validate-develop.sh greps features/ for direct library imports that bypass shared wrappers" — `scripts/validate-develop.sh:51-61` greps only `new PrismaClient(` and `new Redis(`/`new IORedis(`, never imports.
- `dist/development/RED-FLAGS.md:29` "fails if a feature directory exists without a corresponding docs/features/{name}.md" — group 4 (`:105-132`) cross-checks feature-tree rows in the other direction and warns when `docs/features/` is absent, so a directory with no row and no doc passes.
- `dist/development/DEVELOP.md:174` "No throw new Error( in features" listed under "Final gate — automated validator" (`:166`) while `validate-develop.sh:142` emits `warn`, and the exit block returns 1 only on ERRORS, so the gate provably never blocks.
- `dist/development/MAINTAIN-RED-FLAGS.md:38` "The shipped hooks ... do NOT check doc freshness. The defense was name-only." — the class is already a recorded silent-failure pattern, fixed once by hand.
- V2 adds a fifth live misclaim: `validate-scaffold.sh`'s own header comment #4 advertises "Direct third-party imports flagged outside project wrappers" while group 4 is the audit-log check, so the script's self-description has drifted too.

**Why it expires.** A defense written as prose about a script decays every time the script changes and nobody re-reads the prose, and it decays in the worst direction: an agent reads "the validator catches this" and stops defending the rule itself. Prose and script share no identifier, so every future edit widens the gap silently. Four such claims are live now, and the founder's standing rule against unverified claims forbids exactly this class of statement.

**Timeless form.** Every check emits a stable id (`OK [scaffold.audit-log-separate]: ...`) and the scripts support `--list-checks`. Every Defense line and every playbook description of a gate cites an id or says, out loud, "advisory: none (human or agent judgment)". A framework self-test extracts cited ids from all four RED-FLAGS docs and the playbooks, diffs them against the ids actually emitted, and fails both on a citation with no check and a check no document cites; it also asserts the severity verb matches the emitter (`fails` requires `fail(`, `flags`/`warns` requires `warn(`). Prose keeps the narrative and names only the group; the authoritative description lives in the script header the self-test diffs against.

**Intent risk.** Honest labelling will expose that several red flags are judgment-only, which reads as a weaker framework and as permission to skip. Keep force by obliging any advisory flag's playbook step to carry an explicit verification line with an evidence artifact, and by checking only the field and the severity verb, never policing the "why it is insidious" paragraphs that make an agent recognise the pattern mid-task.

**Verifier notes**

- **V1 held.** All four misclaims verified against the scripts; `MAINTAIN-RED-FLAGS.md:38` makes it a recurring pattern rather than four stray typos; the remedy introduces no vendor, tool or era assumption and fits the existing bash/awk/grep floor. Effort M.
- **V2 held.** Confirms all four plus the header-comment drift; notes `RED-FLAGS.md:93` hedges with "best-effort" and is the weakest item; the severity-verb assertion is exactly what would have caught `DEVELOP.md:174`. Scopes the work as ids on roughly fifty emit sites across five validators plus extractor, differ and five rewritten claims. Effort M.
- **V3 held.** Confirms both sides by reading each; fit is squarely framework-layer since both prose and validators ship in `dist/`, and `--list-checks` can enumerate ids with no project to scan. Effort M.

---

## C06 — Cross-references, framework-internal claims and enforcer-to-rule agreement are checked by nothing

- **Kind:** missing-mechanism · **Value:** 5 · **Sources:** bootstrap, conventions-a, conventions-b, development, backend, factory-design
- **Adjusted effort:** reported L / L / L → **L (6-10 days)**, unanimous

**Evidence (agent-reported)**

- `dist/scripts/validate-framework.sh:54` the path check greps only `CLAUDE.md`; `:78` the `#N` check only `Conventions.md`. Convention-to-convention, convention-to-playbook and playbook-to-playbook references are scanned by nothing.
- `dist/conventions/00-reusability.md:32` "See bootstrap/ONBOARD.md Step 3 for the full flow"; `conventions/11-auth-security.md:3-4` "see convention #24 ... #23"; `conventions/06-styling.md:64` "(see #22)"; `bootstrap/ONBOARD.md` routes by #0/#2/#16/#18/#22/#23/#24/#27.
- `dist/conventions/26-pulse-monitor.md:18` "the contract lives in templates/pulse-monitor-spec.md, copied to docs/systems/pulse-monitor.md" and `:40` "The framework does NOT ship a separate audit service" — inventory claims about the framework with full authority and no test.
- `dist/backend/CLAUDE.md:18` "Never return raw arrays from API endpoints" contradicts `backend/conventions/B2-api-design.md:53` "GraphQL's own response format is the envelope"; B3 and B7 appear in no enforcer line, so `B3:20` "Never put authentication or authorization checks inside route handlers" never reaches the always-loaded file.
- `dist/conventions/25-automated-enforcement.md:1` "# Convention: Automated Enforcement" — titled without a number while every sibling carries one, and `conventions/overrides/<N>-<name>.md` filenames downstream key on the ordinal (`EXISTING-PROJECT.md:46/90-91`, `convention-template.md:45`, References templates; `update.sh:201-209` preserves that directory by name).
- V2 ran an independent resolver: of 91 engine-path references in shipped `.md` files, `templates/runbook-commerce.md` (cited by `SCAFFOLD-PLATFORM.md:94`), `frontend/CLAUDE.md` and `frontend/Conventions.md` (cited by `CLAUDE.md:60`, with "frontend" in `update.sh`'s UNIVERSAL_DIRS) do not exist, and `META-BATTLE-TESTING.md:14` still says "28 conventions".

**Why it expires.** Two decay paths. **Link rot:** citations point at playbook files, playbook step numbers, conventions by ordinal and framework-internal paths, all of which move, and nothing fails when they do. **Identity:** a convention's name is its position, cited across conventions, templates, feature trees and downstream override filenames, so insertion, split or regrouping either renumbers and breaks every downstream reference or is avoided because renumbering is too expensive. Meanwhile the routing layer (enforcer list, index, quick lookup) is hand-maintained and already disagrees with the conventions it cites.

**Timeless form.** One coherence self-test, independent of the timeless validator, over every shipped document: resolve every path token, every `#N` or slug reference and every "FILE section Heading" anchor, failing on a dangling target; forbid citing a playbook step by ordinal and check the named heading exists; assert membership (every convention file appears in its index and quick-lookup table and either in an enforcer line or an explicit no-enforcer roster with a reason); assert each enforcer line carries the id of the rule it condenses and that the two agree on the number and on absoluteness; replace "the framework ships / does not ship X" with a capability manifest checked against the tree, keeping #26's negative boundary as a scope rule rather than an inventory statement. Give each convention a stable slug as canonical identity with `#N` as the human display alias, and one alias map that the checks and the updater both read, so a later renumber breaks no downstream citation or override filename.

**Intent risk.** Numbers are the shared vocabulary the founder and every installed project use ("#22 says"), so the slug stays machine-facing with `#N` kept in titles and prose, and the updater migrates existing numeric override filenames once rather than asking projects to rename. Semantic agreement between an enforcer and its rule is mechanically checkable only for the numeric and never/always parts; check those, require the tag, leave the rest to audit. Adding B3 and B7 enforcer lines must not dilute a list that works because it is short.

**Verifier notes**

- **V1 held.** Verified that groups 1-7 cover required headings, unique numbers, backend paths and pulse shapes but resolve no convention-to-convention, playbook or framework-internal claim; confirms the `backend/CLAUDE.md:18` vs `B2:53` contradiction and the unnumbered #25 title against downstream `{N}-{name}.md` filenames. Not a Step 62 re-proposal: Step 62 validates expirable content, not coherence. Effort raised because the candidate adds a slug identity layer, an alias map read by checks and updater, a one-time migration of numeric override filenames in installed projects, and a capability manifest, across ~50 shipped docs. **L**
- **V2 held.** Ran a resolver and found the predicted decay already live and invisible (three dangling engine paths, one stale count). **Required amendment:** the resolver needs two namespaces, because several dangling-looking paths (`scripts/pulse.sh`, `scripts/pack-local.sh`, `conventions/overrides/*`) are correctly project-relative, not engine-relative; a single-namespace check would either fail on them or be relaxed into uselessness. **L**
- **V3 held.** Confirms group 4 checks only section presence and group 5 only filename uniqueness, so the unnumbered title passes while `EXISTING-PROJECT.md:46` mandates ordinal-keyed override filenames; every proposed check is bash/awk/grep over shipped files. **L**

---

## C07 — Hook enforcement is welded to one host's event ABI, and no validator reads hook scripts

- **Kind:** structural-assumption · **Value:** 5 · **Sources:** bootstrap, factory-design, templates-scripts
- **Adjusted effort:** reported M / M / M → **M (3-5 days)**, unanimous

**Evidence (agent-reported)**

- `dist/bootstrap/hooks/pre-destructive-warn.sh:6` "# Claude Code contract (2026):", `:16-17` `jq -r '.tool_name'` and `.tool_input.command` (with a no-jq fallback at `:19-21` keyed to the same field names), `:24` gates on `TOOL = 'Bash'`, `:65` `exit 2`; `post-task-verify.sh:1-7` the same for the Stop event.
- `dist/bootstrap/hooks/README.md:54` "Cursor and other tools have different hook mechanisms. The shell scripts still work; adapt the configuration format" — false at the parsing layer (TOOL resolves empty and the script exits 0 silently), and invisible because scripts are not scanned.
- `dist/bootstrap/hooks/README.md:60` "Claude Code exposes 21 lifecycle events as of 2026" — a vendor API count with a year attached, in instructional text.
- `dist/templates/claude-settings.root.json:9` `$CLAUDE_PROJECT_DIR/bootstrap/hooks/pre-destructive-warn.sh` with PreToolUse/Stop keys, in a `.json` file the timeless validator never scans (`validate-timeless.sh:83-84` scope is `.md` only).
- `planning/ARCHITECTURE.md:170-171` lists "PostToolUse (Write/Edit)" and "PostToolUse (TaskComplete)" — the second is not an event that host exposes, and nothing could detect it.
- `scripts/timeless-allowlist.txt` holds one entry, "Claude Code" in `templates/hooks-spec.md`: Step 62 resolved the vendor name here by allowlisting rather than abstracting the contract.

**Why it expires.** Convention #25's automated enforcement reaches a project only through two scripts that parse one host's event field names and depend on its exit-code semantics, settings location and project-dir variable. When the schema changes or the project moves host, enforcement silently no-ops — the exact failure the README warns about for the wrong settings file — while the framework keeps claiming enforcement it does not have.

**Timeless form.** Define the abstract events the framework needs (after-file-change, before-risky-command, at-task-end, at-session-start) and state every hook rule against those. `bootstrap/hooks/adapters/<host>.sh` is the only place host strings live: it reads the payload, exports a normalized contract (`ARCH_EVENT`, `ARCH_TOOL`, `ARCH_COMMAND`), maps verdicts (`arch_block`, `arch_warn`, `arch_allow`) to that host's codes, and declares config path, format and can-block. The two shipped scripts source the adapter named by References.md's recorded host and contain zero host literals; configs are generated from the binding; a needed event with no binding fails the self-test, and a binding that cannot block makes the framework say "advisory only" instead of claiming enforcement. The self-test feeds a synthetic event, asserts the exit code, and greps hook scripts for host field names so the separation cannot erode. Instructional text names event roles, never a vendor's count or year; hook script comments and `templates/*.json` enter the scan set.

**Intent risk.** The force of these hooks is that they actually block; an adapter layer must not become the reason a block stops firing, hence the synthetic-event verdict test. Abstracting without recording can-block would let the framework claim enforcement it lacks, the honesty line Steps 61 and 62 already drew.

**Verifier notes**

- **V1 held.** Verified both hook scripts, the dated contract comments, the README's two claims, the JSON template's out-of-scope status and the non-existent `PostToolUse (TaskComplete)`; confirms the allowlist-not-abstract premise. The role-based event set plus per-host can-block does not swap one era for another and matches `ARCHITECTURE.md:167`'s existing "hook presence alone is not proof of enforcement". Caveat: extending the scan set to `templates/*.json` and script comments duplicates Step 62 follow-up 3, so only the adapter abstraction is new work. **M**
- **V2 held.** Confirms the host coupling survives the no-jq fallback, which keys on the same field names, and that README:54 is materially false. Two corrections to framing, neither fatal: `hooks/README.md:56` and `ARCHITECTURE.md:166` already state the enforcement gap honestly, so "keeps claiming enforcement it does not have" is weaker than written; and the adapter must not swallow the jq-absent fallback, a real safeguard on minimal machines. **M**
- **V3 held.** Confirms the scan set from `validate-timeless.sh:83-84`, the correct layer split (framework ships the hooks and the claim; the host is a project-recorded specific), and that `test-entrypoints.py` already exists as the harness for a synthetic-event feed. **M**

---

## C08 — One forge, one account, one repo name is the framework's only distribution and promotion channel

- **Kind:** structural-assumption · **Value:** 5 · **Sources:** bootstrap, development, templates-scripts, factory-design
- **Adjusted effort:** reported M / M / M → **M (3-5 days)**, unanimous

**Evidence (agent-reported)**

- `dist/update.sh:76` `FRAMEWORK_REPO=https://github.com/d3r3nic/archetype.git`, `:88` shallow clone of HEAD with no revision argument and no integrity check, `:300-305` the updater replaces itself from that same clone; the only accepted argument is `--project-root` (`:12-18`), with no env override, pin, checksum or signature anywhere.
- The same literal in `dist/bootstrap/ONBOARD.md:20/31/32/42`, `dist/README.md:11/16/17/26`, `dist/inject.sh:126`, `dist/META-BATTLE-TESTING.md:13-14`; `scripts/test-entrypoints.py:100` can only fake an update by rewriting that literal via `git config url.insteadOf`.
- `dist/development/MAINTAIN.md:43` "open an issue in the framework repo" and `:58` "PLAN.md + CHANGELOG.md entry -> dist/ changes" — factory-internal filenames instructing a downstream project.
- `dist/update.sh:143` the CHANGED/NEW preview finds only `-name '*.md' -o -name '*.sh'` while `:197-215` `rm -rf` and re-copies whole directories (UNIVERSAL_DIRS includes `scripts` and `templates`), so `timeless-terms.txt`, `timeless-allowlist.txt`, `test-entrypoints.py`, both `claude-settings*.json` and the whole pulse UI are written without appearing in the diff the operator approves at `:177`.
- `dist/bootstrap/REPOSITORIES.md:11-14` "Source pointers must be sufficient to refresh the installation without maintaining another copy of the rules by hand" — the correct principle, one directory away.

**Why it expires.** Every installed project receives framework change only by anonymous clone of one vendor's hosting, one personal account, one repo name, at whatever HEAD happens to be, with no integrity check and no way to pin, roll back or reproduce. A rename, a forge move, an air-gapped machine or a consumer without access kills the updater in the field, including its ability to replace itself with a fixed version, and promotion debt accumulates exactly when the loop is unperformable.

**Timeless form.** One SOURCE manifest in the engine (transport, location, optional default revision) written by `inject.sh` and read by `update.sh`, overridable by `--source` (URL, local directory or archive as first-class) and `--revision`, recorded in the project's References.md as its update channel; a content digest over the managed file set plus an opaque release id recorded in VERSION-LOG, verified after transfer, so a revision is identifiable without the VCS that produced it. Every shipped command reads the pointer (`$ARCHETYPE_SOURCE`) instead of a literal, with Step 1 giving one copy-pasteable line that sets it. The promotion channel is recorded at install by role (tracker, mailbox, pull request, shared drop) and downstream docs say "report through the recorded channel", naming no forge, account or factory-internal filename; the self-test fails when the channel field is empty. Build the update preview from the same traversal as apply (every regular file, grouped by directory with counts) so consent covers what will be written, and add URL-shaped patterns and the framework's own account and slug to the term list.

**Intent risk.** The hardcoded URL is what makes updating one command for a non-developer founder, and an abstract "recorded channel" can turn a performable action into a shrug. Keep the default resolved from the existing pointer so the common path is byte-identical, make `--revision` purely opt-in with the updater printing how far behind a pin is, and require the recorded promotion channel to be a literal actionable destination.

**Verifier notes**

- **V1 held.** Verified the literal, the absent flags, the self-replace and the preview/apply asymmetry (the `-type f` binds only to the first `find` branch). Work is concentrated in one ~11KB script plus a manifest, six doc literals, a rebuilt preview, a digest in VERSION-LOG and regressions in `test-entrypoints.py`; the default path stays byte-identical. **M**
- **V2 held** — calls it the strongest-evidenced of its batch. **Required amendment:** the self-replace path must be digest-verified before the rename, or the integrity check is bypassed by exactly the file that performs future checks. **M**
- **V3 held.** Confirms consent does not cover what is applied, and that `REPOSITORIES.md:11-14` already states the governing principle; layer fit holds because the pointer and promotion channel are recorded project specifics while reading a pointer is engine behavior. **M**

---

## C09 — Verification steps are written around a credential-less agent and a human with eyes, and accept no evidence

- **Kind:** structural-assumption · **Value:** 5 · **Sources:** scaffolding
- **Adjusted effort:** reported M / M / M → **M (3-5 days)**, unanimous

**Evidence (agent-reported)**

- `dist/scaffolding/SCAFFOLD-PLATFORM.md:107` "the AI agent running this scaffold almost never has the ability to execute a smoke-test on the actual platform ... The agent's responsibility is to PRODUCE the script; the owner EXECUTES it live".
- `dist/scaffolding/SCAFFOLD-BACKEND.md:275` "start the dev server, open the pulse route, confirm all 5 sections render with real data" (same at `SCAFFOLD-FRONTEND.md:271`) — verification by eyeball plus a cached count.
- `dist/scaffolding/SCAFFOLD-MOBILE.md:53` "Have a human review the list against current store submission policies before first build."
- `dist/scaffolding/SCAFFOLD.md:92` "Sessions: [how many discrete AI sessions it took]" — the recorded unit of work is one agent's context window.
- `dist/conventions/18-verification.md:20` "For UI changes, verify the visual output. Describe what the screen looks like or compare to a design."
- Of 39 `**Verify:**` lines across the scaffolding playbooks, none demands an evidence artifact; classes A-F cannot see a capability assertion that carries no number.

**Why it expires.** Two era-bound beliefs are load-bearing: that an agent cannot hold credentials or drive a browser, and that "a session" is a meaningful unit of effort. Both are already unstable. When they flip, the platform playbook's division of labour does not merely read as dated, it forbids work the agent can do and leaves the scaffold's exit criterion blocked on a human who need not be in the loop.

**Timeless form.** Replace the actor assumption with a recorded capability and a mandatory evidence artifact. Bootstrap records in References.md which capabilities the executing agent actually has (credentialed platform access, browser control, device access, payment authority), established by a verified probe, never by the agent's assertion. Every Verify line becomes "actor: any-capable | requires: &lt;capability&gt;" plus the artifact it must leave behind: command and exit code, a saved response body, a screenshot path, or a human sign-off line with date and name. The VERSION-LOG field becomes "Verification evidence" per system instead of "Sessions". A validator class fails any Verify line whose only verb is confirm/check/see/look with no artifact.

**Intent risk.** The current wording guards against an agent claiming a live smoke test it never ran. Moving from "the agent cannot" to "the agent may if capable" removes that guard unless the evidence artifact is mandatory and the capability comes from a verified probe — otherwise a stale limit is traded for a fabrication risk.

**Verifier notes**

- **V1 held.** Evidence verified; the capability assumption is already partly false and none of the 39 Verify lines demands evidence. Two weaknesses, not fatal: `SCAFFOLD-MOBILE.md:53`'s human review against store policy is arguably durable compliance practice, and the "verified probe" is the load-bearing unknown since probing browser control or payment authority is itself host-shaped and risks reproducing C07's coupling. **M**
- **V2 held.** Corrects the title's overstatement: `SCAFFOLD.md:94-95` already requires "verify: &lt;command&gt; exit 0" per system in the same log block as the Sessions line, so the work extends an existing evidence field rather than inventing the idea. **Required amendment:** the human-sign-off artifact must stay a permitted outcome, or the rule becomes unsatisfiable and will be ignored. Also notes `SCAFFOLD-PLATFORM.md:107` leaks "Step 41", a class-B violation outside the validator's scope. **M**
- **V3 held.** Confirms the validator blind spot (classes A-F match names, factory steps, AI statistics, limits, changelog language and notices only) and the layer split: capability values and evidence paths are project artifacts, while "every Verify line names an actor, a required capability and the artifact it leaves behind" is framework character. **M**

---

## C10 — One human steering one agent serially: no authority model, no role-scoped entry, no ownership of shared indexes

- **Kind:** structural-assumption · **Value:** 5 · **Sources:** conventions-b, conventions-a, development, factory-design
- **Adjusted effort:** reported L / L / L → **L (6-10 days)**, unanimous

**Evidence (agent-reported)**

- `dist/conventions/19-steering.md:25` "The human decides architecture ... Understanding is the human's job", `:20` "Present options before implementing ... wait for a decision", `:48` "Present this to the team and wait for explicit go-ahead"; `conventions/15-build-ci.md:44` "cleaned up in the same sprint".
- `dist/development/DEVELOP.md:107` "feature-tree.md update — new row" and `MAINTAIN.md:22` "Append a row to feature-tree.md section Audit Log" — every durable record is one shared markdown file appended by hand.
- `dist/development/FRESHNESS.md:18` "Use version-checked writes and an ownership fence where the source supports them" against `dist/development/TASKS.md:15` "Claim the task through the available ownership mechanism before changing shared work" — no mechanism exists for `feature-tree.md`, `References.md` or `TECHNICAL-DEBT.md`.
- `dist/conventions/03-architecture.md:19` "If code needs sharing, move it to shared (ask first)" — presumes a human in the session to answer.
- `planning/ARCHITECTURE.md:143-150` a single 7-step join order for "New AI agent joins mid-project" and `:181-182` behavioral rules living at one vendor's home-directory path — where `ARCHITECTURE.md:182` itself classifies "Present options before implementing" as a personal behavioral rule belonging in the host's global config, while #19:20 ships it as a convention.

**Why it expires.** The steering model, the onboarding contract and every shared record assume one actor editing one working tree with a human available to answer, and a "team" that holds meetings and runs sprints. The framework's own guidance already encourages worktrees for parallel work, and this repository is itself developed by writer and auditor agents plus narrow subagents. Under more than one actor the losing row vanishes silently from `feature-tree.md`, precisely the silent-failure class the catalogues exist to prevent, and "present options and wait" already contradicts how some operators want to be addressed, which is evidence it encodes an interaction taste rather than an invariant.

**Timeless form.** Record authority and topology instead of assuming headcount: the accountable approver for scope, architecture and breaking changes (never the agent doing the work), the review topology (solo operator, team, supervising fleet), the escalation form the authority wants (one recommendation versus options), and the project's unit of independently reviewable change. Per-role entry sets replace the single ordered read — full-context agent, narrow subagent (enforcer plus the one system doc, no editing shared maps), reviewer (enforcer, conventions, the diff), human — with the enforcer mandatory in every role. Shared indexes become generated views over per-feature and per-cycle fragments, with a gate asserting exactly one entry per feature so a lost row is detectable, and any map that stays hand-edited carries the claim sequence the framework already demands of tracked work (read the revision, write fenced to it, re-read to confirm) plus a narrative section kept by hand. "Ask first" resolves to the owner named in the coordination artifact, or the human if none; the behavioral-rules path resolves through the host adapter (C07).

**Intent risk.** Written loosely this licenses agents to approve their own work, to batch twenty unverified changes by setting the unit conveniently, or to bury onboarding in ceremony. Two clauses stay mandatory and non-dialable: the approver is never the author, and a human remains the accountable authority of record even when an agent reviews. The obligations must be no-ops for a single actor, and generated indexes must keep the human-readable page that makes `feature-tree.md` useful.

**Verifier notes**

- **V1 held.** Verified the internal contradiction (FRESHNESS/TASKS demand a fence; DEVELOP/MAINTAIN specify hand-appended rows with no mechanism for the three shared files) and notes the taste argument is stronger than stated, since `ARCHITECTURE.md:182` itself classes the steering line as a personal behavioral rule. Effort raised: this is a restructuring touching conventions, all three playbook phases, templates and the pulse inspector's parse contract (`validate-framework.sh` group 7 locks the feature-tree column shape, so generated views must preserve it). **L**
- **V2 held.** Notes the founder's standing directive ("never a menu of options") contradicts `19-steering.md:20` in this very session, first-hand support for the taste claim. **Required amendment:** making "the project's unit of independently reviewable change" a dial would dissolve `19-steering.md:18` ("One change at a time. Verify between changes."), so the unit must carry a mandatory verification gate per unit (`DEVELOP.md:110`'s commit-on-gate-passed rule) — the dial controls granularity, never whether verification happens. **L**
- **V3 held.** Confirms the layer split (headcount, approver identity, topology and escalation form are recorded specifics; "the approver is never the author" and per-role entry sets are framework character) and that generating an index from per-feature fragments is awk work of the kind `pulse-inspect.sh` already does. Raised to **L** because it restructures the artifact the inspector parses under contract. **L**

---

## C11 — One VCS, one forge workflow and one hook lifecycle are written as the rule, including the literal bypass flag

- **Kind:** structural-assumption · **Value:** 5 · **Sources:** conventions-a, conventions-b
- **Adjusted effort:** reported M / M / M → **M (3-5 days)**, unanimous

**Evidence (agent-reported)**

- `dist/conventions/02-git.md:1` "# Convention #2: Git & Version Control", `:12` "Pre-commit hooks", `:13` "A pull request template", `:20` "Never commit secrets, environment files, or credentials", `:21` "Never force push to the main branch", `:22` "Never skip pre-commit hooks", `:32` "Using --no-verify to skip failing pre-commit hooks".
- `dist/conventions/25-automated-enforcement.md:22` "Never bypass hooks with --no-verify except for documented emergencies" and `:12` "Pre-commit hooks: run linter + formatter + type check before commit lands" (cited as `:13`).
- `dist/conventions/15-build-ci.md:15` "Preview deployments that create a unique URL for every pull request".
- `dist/conventions/23-app-security.md:42` "API key hardcoded in source code and committed to git" and `:54` ".env file with DATABASE_URL=... committed to git".
- `dist/scripts/timeless-terms.txt:270-272` GitHub, GitLab and Bitbucket are terms while `git` itself, `--no-verify` and "pre-commit" are not; verifiers ran `validate-timeless.sh` on `02-git.md` and `23-app-security.md` with explicit arguments and both pass all six classes.

**Why it expires.** The durable content is: a verified change becomes one atomic attributed revertible save point; the shared integration line is never rewritten once published; a gate runs before the save point and before the merge, and a bypass is never silent. Everything else is one tool's vocabulary plus one forge era's review object. The named bypass flag is the sharpest case: the rule forbids a literal CLI flag. A project on another VCS, a patch-mail or merge-queue workflow, or a product where preview URLs are meaningless reads rules it cannot follow literally, and an agent then invents a mapping or declares the convention broken.

**Timeless form.** Retitle to version control and save points and state the rules as properties: each verified change is one atomic save point whose message says what and why; published history is append-only; a write-time gate runs before a change enters history and a merge gate before it enters the shared line, and bypassing either requires a recorded reason and a follow-up fix; a broken file is fixed in place, never forked to a variant; every review unit is reviewable in its built form. A References.md "Version Control" section records the VCS, the integration-line name, each gate's attachment point and command, the literal bypass mechanism the project must never use, the review-unit object, and how a review unit is reviewed (URL where the product is served, build artifact or screenshot set where it is not). A named script lints the recorded literals, so the prohibition stays greppable while only the spelling becomes project data; today's vocabulary stays as the dated example in Research Notes, and git, the forge names and `--no-verify` join the term list so it cannot creep back.

**Intent risk.** This is the most concrete convention in the set and concreteness is the point: "never force push to main" and "never --no-verify" leave an agent no room, while role-named gates read as vaguer. The recorded-mechanism slot preserves force — the project writes the exact flag and branch name, the enforcer greps those, and the absolute prohibition plus the no-silent-bypass clause stay verbatim.

**Verifier notes**

- **V1 held.** Ran the validator read-only on two cited files: both pass, confirming class A cannot see the git literals. Expiry is present-day, not speculative: a GitLab project calls the review object a merge request, a mobile or CLI product has no per-PR preview URL, a non-git VCS has no `--no-verify`. Two corrections: the proposed References section partly exists (`references-frontend.md:55-59` records commit convention, branch strategy, pre-commit hooks, budget, branch protection), so the new slots are the VCS identity, integration-line name, bypass literal, review-unit object and review method; and the rewrite footprint is small (20 whole-word "git" hits in 7 in-scope files, 3 `--no-verify`, 1 force push, 2 pull request), so cost sits in the new enforcer. **M**
- **V2 held.** Confirms the clean validator run and that `postgres` at `23-app-security.md:54` passes because class A is case-sensitive. **Two conditions on acceptance:** the unified bypass clause must not adopt #25's looser "documented emergencies" wording as the floor for #2's absolute "If a hook fails, fix the issue, don't bypass it"; and `02-git.md:20`'s "Never commit secrets, environment files, or credentials" must survive verbatim. Judges abstracting the VCS itself the weakest half and the forge-era review object the live half. Caution: in this repository "enforcers" means `CLAUDE.md`/`Conventions.md`/`README.md`, not scripts, so "enforcers lint the recorded literals" needs a named script owner. **M**
- **V3 held.** Confirms the citations (with `25-automated-enforcement.md`'s pre-commit bullet at `:12`, not `:13`) and that `STEP-62:33` exempts languages, standards and ship targets but never considers the VCS or a CLI flag, so this gap is unexamined rather than decided. Scope small and bounded (19 whole-word "git" hits in the default scan set). **M**

---

## C12 — Bootstrap's halt-before-production gates live in the session's memory, and the scaffold validator reads a deferral as a negative answer

- **Kind:** missing-mechanism · **Value:** 5 · **Sources:** bootstrap
- **Adjusted effort:** reported M / M / M → **M (3-5 days)**, unanimous

**Evidence (agent-reported)**

- `dist/bootstrap/ONBOARD.md:144` "Deploy gate: if regulated-data is default-assumed-yes and never affirmatively answered, scaffolding and deploy (Phase 2+) must halt."
- `dist/bootstrap/RED-FLAGS.md:77` "default to the most-likely interpretation, LOG the assumption in VERSION-LOG as an open pre-production gate" with `RED-FLAGS.md:44-49` the Deploy Gate, against `ONBOARD.md:458` "After bootstrap completes, update VERSION-LOG.md".
- `dist/scripts/validate-scaffold.sh:179` the not-regulated pattern includes `skipped|deferred`, so a deferred answer sets `REGULATED=0` and the else branch at `:203` prints "pass: no regulated data declared (or explicitly N/A) - audit log check skipped".
- `dist/scripts/` holds validate-scaffold, -develop, -maintain, -migration, -framework, -timeless and **no** validate-bootstrap, so nothing downstream can tell a gate was left open; `validate-scaffold.sh` touches VERSION-LOG only at group 7, where a missing Scaffold entry is a warn.

**Why it expires.** The gate assumes one continuous session in which the agent that ran discovery is the agent that scaffolds, with the interview still in context. Context limits, multi-agent hand-offs, resumed sessions and different hosts all break it, and nothing in the repository can then tell that a compliance gate was left open. Worse, the one machine check downstream actively inverts an unresolved gate into a pass.

**Timeless form.** Make the discovery record the first artifact bootstrap writes, not the last: an append-only set of one-line machine-readable entries ("Gate: regulated-data | answer: default-assumed-yes | resolved: no | set: &lt;date&gt;") covering regulated data, budget, stack preference, product shape and the turn-budget default, written at the moment each group is answered or defaulted. Add `scripts/validate-bootstrap.sh` that fails while any gate is unresolved or any researched line is unstamped, have `validate-scaffold.sh` call it first, and remove `skipped|deferred` from the not-regulated pattern so a deferral can never read as a negative answer. VERSION-LOG keeps the narrative; the gate lines keep the state, so a fresh agent in a fresh session reaches the same verdict as the interviewer.

**Intent risk.** The intent is a hard halt before production on an unanswered compliance question; today that halt is advice to a remembering agent. Care is needed not to create a second source of truth competing with VERSION-LOG, and the resolution line must carry the answer text it was resolved with so nobody writes "resolved: yes" without an answer.

**Verifier notes**

- **V1 held.** Confirms the inversion and the absent validator. **Correction:** `ONBOARD.md:144` does instruct recording the gate when it is opened, not only at Step 6, so the write-ordering claim is weaker than stated; the defect is the format and the consumer (prose no script can read) plus the inversion. **M**
- **V2 held.** Traces the pattern: the negation filter at `:179` is applied before the affirmative regime match at `:182`, so "Regulated data: deferred" bypasses the whole regulated-data group. Same correction about recording timing. Names the S-sized core (remove `skipped|deferred`, treat an unresolved gate as regulated until answered) inside the M-sized deliverable. **M**
- **V3 held** — calls it the strongest-verified candidate of its batch. Confirms group 4b's looser grep is not the gate, and that the deliverable is a new validator, a gate-record format written at answer time in two playbooks, the pattern fix and a regression so a deferral can never pass again. **M**

---

## C13 — The gate's reach is three closed lists, and the planning catalog the conventions are pressed from was never remediated

- **Kind:** missing-mechanism · **Value:** 5 · **Sources:** templates-scripts, factory-design, conventions-b
- **Adjusted effort:** reported L / L / M → **L (6-10 days)**, majority L

**Evidence (agent-reported)**

- `dist/scripts/validate-timeless.sh:83-86` the default scope is a literal glob list, so `AGENTS.md`, `META-BATTLE-TESTING.md`, `libraries/`, the playbooks, `templates/*.json` and `templates/pulse-ui/` are unscanned by default.
- `dist/scripts/timeless-terms.txt:24` "Add a term when a review finds a name the list missed ... the list only grows" — a name invented in 2029 passes silently while the check prints OK; `templates/references-mobile.md:161/176` vendor names and Info.plist keys pass today (verifiers confirmed by running the validator on that template).
- `dist/conventions/23-app-security.md:54` `DATABASE_URL=postgres://admin:password123@...` passes because class A is case-sensitive while `timeless-terms.txt:114` lists `Postgres` — URL schemes, package paths, command lines and env values are where lowercase vendor names live.
- `planning/CONVENTIONS-CATALOG.md:150` "Runtime validation with Zod at boundaries", `:307` "ESLint strict + Prettier", `:336` "AI over-comments 90-100% of the time", `:356` "Small focused files (&lt;300 lines)" — inside the `Includes:` lists that are the prescription conventions are authored from, while `:7` claims the catalog's names are only provenance.
- `planning/STEP-62-TIMELESS-CONVENTIONS.md:162` "Scripts and UI assets are not scanned."

**Why it expires.** Both axes of detection are lists written on the day the gate shipped, so coverage decays by addition: every new file type and every new vendor name starts outside the check, and the output stays green either way. And because `planning/` is out of scope, the mould is dirtier than the product: the next author extending a convention from the approved map reintroduces exactly what Step 62 deleted, which is how a remediated product silently un-remediates.

**Timeless form.** Invert both defaults. **Scope:** walk every tracked text file in the product and the factory, with per-file-type profiles (prose, playbook, script comments, JSON, planning) deciding which classes apply, and an exclusions registry carrying the allowlist's hygiene — one written justification per entry, an entry matching nothing fails, and the run prints the exclusion count. **Detection:** add an unknown-proper-noun class (CamelCase, dotted identifiers, SCREAMING_SNAKE prefixes, capitalized words not at sentence start) checked against a vocabulary of what is legitimately nameable (languages, open standards, regulations, target platforms, the framework's own nouns), shipped as a warning each hit must resolve by entering the vocabulary with a reason or being reworded; and make class A case-insensitive only inside identifier contexts (`<term>://`, package or module paths, command lines, env values) so ordinary-word product names stay out of prose matching. Restructure every catalog entry into Intent (paste-ready WHAT and WHY) and "Dated sources and era examples" opening with the dated notice and an As-of, then run the planning profile over it.

**Intent risk.** A blunt inversion over playbooks plus an unknown-noun class will be noisy and could push authors toward vague prose or blanket exemptions. The nuance belongs in the profiles, not the scope; Research Notes and dated blocks stay fully exempt; must-justify and must-match keep a blanket exemption visible. In the catalog, provenance strengthens rather than disappears: every name keeps its place, now with the date the belief was held.

**Verifier notes**

- **V1 held.** Ran the validator on `23-app-security.md`: clean despite `postgres://`, demonstrating the case-sensitivity hole. Confirms the catalog finding as the strongest and most novel part, including that `:336` and `:356` are verbatim the items Step 62's remediation table records deleting from #16, #1 and #17. **Two qualifications:** the scope-inversion half substantially duplicates Step 62 follow-ups 2 and 3, so the carrying content is the new detection machinery plus the catalog remediation; and the `references-mobile.md:161/176` evidence is weakest, since HealthKit, Google Fit and `NSCameraUsageDescription` are target-platform API facts Step 62 deliberately excluded. Raised to **L**: inverting scope entails remediating the 234 findings Step 62 already measured in bootstrap/scaffolding/development plus new-class findings, and restructuring 26 catalog entries in a 515-line file, before any new machinery is written.
- **V2 held.** Demonstrated both holes by running the validator on `23-app-security.md` and `references-mobile.md` (both pass). Same Step-62 overlap deduction; recommends pitching on the unknown-proper-noun class, the exclusions registry, identifier-context case handling and bringing `planning/` under the gate. Raised to **L**: four separate pieces of work, each M on its own.
- **V3 held.** One run over `AGENTS.md`, `references-mobile.md`, `02-git.md` and `23-app-security.md` reports every class OK, demonstrating scope and case holes together. **Fit objection, not a refutation:** the catalog restructure is factory housekeeping — `planning/` is history and `CONVENTIONS-CATALOG.md:7` is an explicit recorded decision that its names are dated source notes — so a planning profile may check that each entry's dated block is marked, but rewriting every entry should not gate the product-side work. Raised to **L** for the vocabulary curation needed to drive the warning count to zero (a week-plus). (Reported size M in the candidate, L in this verdict's text.)

---

## C14 — One project's hard-won specifics were hoisted into the universal frontend playbook as unconditional steps

- **Kind:** structural-assumption · **Value:** 5 · **Sources:** scaffolding
- **Adjusted effort:** reported L / M / M → **M (3-5 days)**, majority M

**Evidence (agent-reported)**

- `dist/scaffolding/SCAFFOLD-FRONTEND.md:52` "This pattern is hoisted to framework level because every CMS-driven consumer site faces the same need" (Step 1b Global Site Config).
- `:212` "For any operation that takes seconds-to-minutes ... build a subprocess/job registry + SSE stream pattern" and `:223` "Every value that reaches spawn(), exec(), or any shell-like API must pass a safe-ident regex first" — in a frontend playbook, where most projects spawn nothing.
- `:247` "When placing multiple tenants behind shared ingress, verify the vendor allows the backend/NEG/service primitives to cross project/boundary lines", `:248-249` deciding the multi-tenant boundary by who pays the vendor bill, `:250` DNS pointing at a shared static IP, `:254` "Vendor IAM defaults shift across epochs".
- `dist/scaffolding/RED-FLAGS.md` red flag 1, whose Defense (`:11`) states that "within a shape-specific playbook, every step applies unless explicitly marked 'skip if X' with X being a REFERENCES.md fact, not an agent judgment".

**Why it expires.** These steps encode the product shape and hosting topology of the project that discovered them, so as unconditional steps they instruct every future frontend — a dashboard, a game client, an internal tool — to build a site-config module, an SSE job registry and a subprocess boundary it has no use for. They also date fastest of anything in the surface, because ingress, certificate and IAM behaviour is vendor-epoch specific, and nothing marks where they came from.

**Timeless form.** Every hoisted step carries two mandatory lines: "applies-when:" stated in References.md vocabulary (References lists a CMS content source; References lists an operation whose duration exceeds the request timeout; the project shells out) and "provenance:" naming the shape it came from and the date observed. Steps whose trigger is a product shape rather than a universal obligation move out of the shape playbook into a profile the router loads only when the trigger is declared, while the durable kernel stays in the playbook — Step 10d's kernel is "validate at the boundary of any capability that executes", not "spawn/exec". The trigger must be a References.md fact, never agent judgment, and the deployment lore keeps its full text in the profile rather than being summarised away. This is the step-level counterpart of the "Applies when" sections Step 62 added to #26 and #28.

**Intent risk.** Unconditional steps are what stopped these lessons being forgotten, and a trigger is also an exit an agent can talk itself through. Keeping the trigger in References.md closes that exit; summarising the ingress bullets would be the other failure, since each reads as incidental and each cost someone a day.

**Verifier notes**

- **V1 held.** Notes the strongest evidence is the one the candidate cites only glancingly: red flag 1's Defense already specifies the exact mechanism proposed, so this is an internal inconsistency in the shipped product rather than a new convention. Step 1b is the clearest case, its only conditionality living in a rationale sentence the red flag instructs the agent to ignore. The IAM bullet itself concedes vendor epochs while nothing records shape or date. Effort **L**: the applies-when/provenance lines are a day, but moving shape-specific steps into router-loaded profiles touches `SCAFFOLD.md`'s routing and all four shape playbooks.
- **V2 held.** **Evidence correction:** red flag 1 is glossed backwards — it is about agents *skipping* shape-tagged steps, not treating steps as mandatory; the supporting sentence is its Defense, which cuts in the candidate's favour. Frames the contribution as the playbook half of Step 62 follow-up 1 plus the mandatory provenance line and References-sourced trigger, not a new idea. **M**
- **V3 held.** Same gloss correction. Confirms a validator can require applies-when and provenance under every step in a profile file (a grep), and that Step 62 set the precedent with "Applies when" on #26 and #28. **Tension to manage:** insisting the ingress lore keeps its full text means the framework still carries vendor-epoch specifics, so the provenance line must carry an as-of date and the profile must read as a dated zone. **M**

---

## C15 — The rulebook's identity, the installer's check and the project-owned additions file are all named for one AI vendor

- **Kind:** structural-assumption · **Value:** 5 · **Sources:** templates-scripts, factory-design, bootstrap
- **Adjusted effort:** reported L / L / L → **L (6-10 days)**, unanimous

**Evidence (agent-reported)**

- `dist/AGENTS.md:4` "Put project-specific guidance in CLAUDE.md.additions" (and `:6` routes the rules themselves to `CLAUDE.md`) — the neutral entry point points at the vendor-named, never-overwritten, project-owned file; `CLAUDE.md.additions` is referenced 12 times across 8 product files.
- `dist/inject.sh:96` "copied: CLAUDE.md -> project root (auto-loaded by Claude Code)"; `dist/templates/global-claude.md:3` "Place this at ~/.claude/CLAUDE.md."
- `dist/scripts/validate-migration.sh:23` treats the presence of `CLAUDE.md` as proof of installation; V1 adds `validate-framework.sh:21/28` and `validate-timeless.sh:49` locate the root the same way — three scripts, not two.
- `planning/ARCHITECTURE.md:56` and `:220` "at root (auto-loaded by Claude Code)" with no mention of `AGENTS.md`, so the design doc and the product disagree about the entry point.
- `dist/scripts/timeless-allowlist.txt` holds exactly one entry, which exists only because of this coupling (the host's settings format).
- V1 adds: the vendor name is also in the shipped hook config filenames (`templates/claude-settings.root.json`, `claude-settings.injected.json`) and their `$CLAUDE_PROJECT_DIR` command paths, and `templates/*.json` is outside the validator's glob. The marker `<!-- archetype-managed-entrypoint -->` already exists on `AGENTS.md:1` and is what `update.sh:98`, `validate-framework.sh:208` and `test-entrypoints.py` check, while `dist/CLAUDE.md` carries no marker — so the vendor-named file is the one still identified by name.

**Why it expires.** The primary artifact is named after one 2025 product, the installer announces that product's auto-load behaviour, validators treat the filename as proof of installation, and the one permanent project-owned file carries the vendor's name into every downstream repository forever. Supporting a second host already cost a numbered step and left the architecture doc out of date; the next host, or a rename by either vendor, costs another step, another divergence, and a hand-edit of the rulebook filename, the hook JSON and two scripts in every installed project.

**Timeless form.** One framework-owned canonical enforcer (`archetype/ENFORCER.md`) holds the rules; every host-named root file is a generated copy, identical in body below the centrally-managed banner, written from a `hosts/<host>/` adapter that declares entry filename, whether the host auto-loads it, the global-preferences path, the hook binding and blocking semantics. `inject.sh` and `update.sh` manage shims by registry row, and `CLAUDE.md` stays one of them so nothing regresses today. Validators detect an installation by the managed-entrypoint marker, not a filename. The project-owned additions file is renamed for its role (`AGENTS.md.additions` or `INSTRUCTIONS.additions.md`), with the installer relocating any legacy host-named file under the preservation discipline and never deleting it, and `scripts/test-entrypoints.py` gaining a case that the legacy name still loads after rename. Rule text and `ARCHITECTURE.md` say "the enforcer" and "the index"; only the registry names files; the self-test asserts every generated copy matches the canonical source; `templates/*.json` enters the scan with the adapter directory the one justified exclusion.

**Intent risk.** The rules live in the host-named file because the host loads it unasked; a thin pointer would add a hop some hosts follow unreliably, so generate full copies and assert they match. Two things must not weaken: the additions file stays project-owned and update-proof, and exactly one place holds entry-point additions. A rename without the legacy-file test case would orphan existing installations' guidance, the one failure this surface treats as unforgivable.

**Verifier notes**

- **V1 held** — "the candidate whose expiry is least arguable", since the cost has already been paid once (Step 61 added a second host and left the architecture doc behind). **Evidence correction:** "no mention of AGENTS.md anywhere in planning" is false (`planning/STEP-61-TASK-RULEBOOK.md:3/13`, `planning/CHANGELOG.md:15`); the accurate narrower claim is that `ARCHITECTURE.md:56/220` are stale. Effort **L** or beyond.
- **V2 held.** Same correction. Two facts make the proposal cheaper and safer: the managed-entrypoint marker already exists as a convention, and `test-entrypoints.py` already covers existing-CLAUDE.md preservation, the case-variant rename, `.additions` survival across update and the pre-AGENTS legacy export, which is where the demanded legacy-rename case belongs. **That test must land before the rename, not after.** **L**
- **V3 held.** Same correction, and adds: `validate-migration.sh:23` keys on the filename while three other places key on the marker, which is the concrete defect. **Tighten one wrinkle:** a generated copy cannot be both byte-identical to the canonical enforcer and carry an inserted banner — specify the assertion as body-identical below the managed banner, or the self-test is unsatisfiable. **L**

---

## C16 — Rules rest on undated beliefs about tool and model behaviour, and toolchain red flags cannot age out

- **Kind:** expirable-content · **Value:** 5 · **Sources:** conventions-a, conventions-b, backend, bootstrap, scaffolding, factory-design
- **Adjusted effort:** reported L / L / L → **L (6-10 days)**, unanimous

**Evidence (agent-reported)**

- `dist/conventions/06-styling.md:5` "AI hardcodes visual values constantly"; `conventions/00-reusability.md:5` "AI duplicates more than humans"; `conventions/13-performance.md:25` "the most common performance mistake AI makes"; `conventions/22-design-system.md:25` "at a very high rate".
- `dist/conventions/17-context.md:5` "AI performance degrades as context fills ... the middle gets lost"; `conventions/18-verification.md:5` "Verification is the bottleneck in AI development"; `:20` "Describe what the screen looks like".
- `dist/backend/conventions/B1-database.md:31` "AI does this at an extremely high rate" and `B4-logging.md:5` "AI defaults to console-level output for everything" — verifiers ran the validator over the nine backend files: clean, because class C needs a number on the line.
- `dist/bootstrap/ONBOARD.md:95` and `bootstrap/LEARNING-PROJECTS.md:13` "solo + enterprise-infra = learning intent &gt;80% of the time" — an unsourced statistic about humans, invisible to class C's AI-only trigger list.
- `dist/scaffolding/RED-FLAGS.md:112/118/124` flags written against one transform toolchain's behaviour, with `:120` "dated example; verify against the current transpiler" the only admission, and only in the fix; `planning/ARCHITECTURE.md:199/211` "It is how AI scopes its context window" and "AI cannot self-correct across large batches".

**Why it expires.** Step 62 stripped the statistics and kept the beliefs (`STEP-62:23`), so the contingent premise is now the law and nothing can detect it. Each is an empirical claim about one generation of tools (lost-in-the-middle attention, inability to see rendered output, duplicate-component blindness, generation-cheap verification-expensive economics, scarce context), and the audience is the model itself, which makes these the least verifiable and fastest-staling claims the framework can make. When a premise becomes false the rule survives with a reason that is visibly wrong, a reader who finds the rationale false discounts the rule, and a toolchain flag becomes a ghost instruction.

**Timeless form.** Separate invariant from premise. Each convention's Principle states the engineering invariant with no actor ("duplication drifts from its source; one source cannot"; "visual values written in place cannot be re-themed"; "one call per item in a loop is the characteristic scaling failure of data access"; "this failure is invisible at test data volume"), and a required block carries the contingency: "## Observed failure mode" opening with the dated notice plus "As of: YYYY-MM", or a two-line "Symptom: &lt;observable behaviour&gt; / Check: &lt;how a project observes whether the tool in use still shows it&gt;". Add the section to `templates/convention-template.md` and to `validate-framework` group 4's required loop, and widen class C from numbers-on-AI-lines to behavioural and heuristic assertions with no number appearing outside the dated zone. Toolchain red flags gain "observed:" with date and toolchain, "still-applies-if:" a behavioural probe whose output the agent pastes, and "retire-when:"; a false probe is recorded as not-applicable in References.md with that output rather than implemented. One factory-side register maps each capability assumption to the rules resting on it, the durable reason those rules keep without it, and the observation that retires it.

**Intent risk.** These sentences are the framework's persuasion and encode real scars: "AI does this at an extremely high rate" is why a reader takes N+1 seriously. Keep the observation verbatim and adjacent rather than deleting it, keep the obligation unconditional in the rule line with the contingency only in the premise block, and default an unobserved symptom to "assume it still holds" so a missing check never weakens a rule. Two further risks: a cheap wrong probe retires a flag that still bites, and an author may read "era-bound" as licence to delete a rule whose other reasons still hold — hence the register's two columns.

**Verifier notes**

- **V1 held.** Confirms class C needs a number or multiplier on an AI-trigger line and cannot see the surviving beliefs; not a re-proposal since follow-up 2 only extends scope. Two honest weaknesses: for in-scope conventions the rule line is already actor-free, so an expired premise costs a visibly-wrong rationale rather than a broken rule; and widening class C to number-free behavioural words would fire on almost every Principle, so that half must be sequenced after the premise blocks exist. **L**
- **V2 held.** Reproduced the detection gap by running the validator over the nine backend files (clean). Two weaknesses short of refutation: the widened triggers ("AI", "agents", "models", "pattern", "usually", "intent") fire on legitimate scoping prose — `17-context.md:5` and `18-verification.md:5` have no actor-free restatement, and `CLAUDE.md`/#16 use "intent" as a rule word — so the gate needs an exemption design or authors will strip rationale to pass it; and letting a toolchain flag be recorded not-applicable on a probe trades a standing safeguard for project honesty, acceptable only with a behavioural probe and pasted output. **L**
- **V3 held.** Same reproduction (explicit-argument run on `B1`, `B4`, `backend/CLAUDE.md` passes). Confirms the dated-zone precedent (Research Notes) and that group 4 plus a template section is the mechanism. **Narrowing needed before build:** the words "pattern" and "intent" would fire on legitimate lines ("N+1 query patterns", "explain INTENT and CONSTRAINTS"); actor nouns plus heuristic hedges are the defensible trigger set. **L**

---

## C17 — The gates name one era's vendors and one repo layout as their detection logic, and report OK when they are absent

- **Kind:** structural-assumption · **Value:** 5 · **Sources:** development, templates-scripts, scaffolding
- **Adjusted effort:** reported L / L / L → **L (6-10 days)**, unanimous

**Evidence (agent-reported)**

- `dist/scripts/validate-develop.sh:51-58` "# Known shared-class getters: PrismaClient (db), Redis/IORedis, pino (logger)" then greps `new PrismaClient(` and fails with "use getDb() from src/shared/db/", while `:66` prints "no direct shared-class instantiation in features/" when no hit is found — a green line asserting something never tested; `:141` greps `throw new Error` over `.ts/.tsx/.js` only (and the find list includes `.py` and `.go` while the patterns are JS-only).
- `dist/scripts/validate-scaffold.sh:322` fails unless one of four hook filenames exists, `:271` greps seven named migration commands, `:265` looks in four CI config paths, `:353` greps one tracing SDK's symbols; env validation is accepted only at five or six guessed filenames or four symbol names; `:21-24` guesses `SRC_DIR` from four hardcoded directory names; `:6` declares its checks "categorical, not prescriptive".
- `dist/development/RED-FLAGS.md:17` "Similar checks for other shared classes as the framework discovers them" — a growth model that trails the ecosystem forever.
- `dist/scaffolding/SCAFFOLD-FRONTEND.md:61/89` `src/shared/ui/` as the literal enforcement boundary; `SCAFFOLD.md:56-57` `import { NotFoundError } from '@/shared/errors'` inside the universal system-doc template; `SCAFFOLD-PLATFORM.md:125` "No validator".
- `dist/CLAUDE.md:35` "Never instantiate a shared getter's underlying client yourself" — the rule is already vendor-free while the gate that judges it is not.

**Why it expires.** Step 62 removed these names from the prose and left them as live FAIL conditions. A project that obeys the timeless convention with a different ORM, cache, logger, hook manager, migration tool, CI host or language passes the import check vacuously and hard-fails the scaffold gate, which teaches the agent that gate failures are normal. When the products fade the gates stop enforcing anything while still printing OK, which is worse than no gate and violates the framework's own no-unverified-claims rule.

**Timeless form.** Invert ownership: the project declares, the gate reads. A References.md "Layout" section records source roots, shared module root, wrapper boundary, test-file convention, env-validator symbol, dev-guard expression, example-env filename, hook command, migration command and CI config paths; "Foundational Systems" records per system the module path, the accessor symbol and the forbidden-direct-construction pattern the project actually chose. Every gate builds its patterns from that registry and reports three outcomes instead of two — PASS, FAIL, and NOT-VERIFIABLE which fails closed when the registry is absent or its declared accessors are not found — never a silent OK; each system's own "## How to Verify" command must have run and exited 0, which is also how the gate-less platform shape gets a gate. Bootstrap must fill every key or mark it N/A with a reason. Vendor literals survive only inside dated comment blocks, and `scripts/*.sh` enter the timeless scan with one allowlisted adapter path.

**Intent risk.** The concrete names find more than a generic check does, so the teeth move from the framework to the project's honesty and a thin registry buys a green check cheaply. Fail closed on an absent or unverifiable registry, have the scaffold phase populate it so the easy path is a real registry and the lazy path is a visible FAIL, and require a bootstrap completeness check or a working single-ecosystem gate becomes a soft gate for all of them.

**Verifier notes**

- **V1 held.** Verified every line; two findings strengthen it (the script's own "categorical, not prescriptive" self-declaration, and layout inferred from four hardcoded directory names rather than declared). The gates already read References.md for gating conditions (groups 6c, 6d), so the inversion extends an existing pattern, and `references-backend.md:41` already records per-system Location/Usage. **L**
- **V2 held.** Confirms the vacuous PASS is the verified core, and that group 5's throw-new-Error check warns rather than fails, which the candidate does not overstate. The three-outcome form with fail-closed NOT-VERIFIABLE is strictly stronger than today's silent OK. **L**
- **V3 held** — "strongest-evidenced candidate" of its batch. Notes the templates already have the shape to extend (`references-backend.md` "## Foundational Systems" at `:41`, "## Folder Structure" at `:118`) and that group 6d already gates on a References.md declaration, so the inversion has in-repo precedent. **L**

---

## C18 — Playbook steps are hand-numbered positions and phases are one-time, so order and re-entry both decay

- **Kind:** structural-assumption · **Value:** 5 · **Sources:** scaffolding, factory-design
- **Adjusted effort:** reported L / L / L → **L (6-10 days)**, unanimous

**Evidence (agent-reported)**

- `dist/scaffolding/SCAFFOLD-FRONTEND.md` headers run Step 10 (`:158`), Step 11 (`:182`), then 10b (`:196`), 10c (`:210`), 10d (`:221`), 11a (`:225`), 11b (`:259`), 12 (`:273`) — the letter-suffix chain is what insertion-without-renumbering already cost.
- `dist/scaffolding/SCAFFOLD-MOBILE.md:7` "Most Step 0-11 of SCAFFOLD-FRONTEND.md apply identically" — a range into a file whose steps are neither contiguous nor in order; `SCAFFOLD-BACKEND.md:183` "Idempotency (Step 10)" and `:198` "Rate limiting (Step 11)".
- Validator run on the surface, reproduced by all three verifiers: "FAIL: [B] scaffolding/SCAFFOLD-BACKEND.md:136: factory step reference: ## Step 10 — Idempotency" plus nine more class-B hits from the playbook's own headers — the playbooks' two-digit headers are what blocks pointing the validator at playbooks mechanically.
- `dist/scaffolding/SCAFFOLD-BACKEND.md:3` "ordered so each system can be built and verified before the next depends on it" — the real information the numbers carry.
- `planning/ARCHITECTURE.md:101` "Phase 1: BOOTSTRAP (one-time)", `:107` "Phase 2: SCAFFOLD (one-time)", `:112` the fixed twelve-stage order; `planning/STEP-62-TIMELESS-CONVENTIONS.md:160` a text game marked 8 of 16 foundational systems not applicable.

**Why it expires.** Position-as-identity makes every new system the framework learns either a renumber that breaks every cross-reference and every downstream VERSION-LOG, or another letter suffix. The fixed order is also one product shape's: a game struck out half of it, a CLI or pipeline would strike more and add systems the order never names. "One-time" is the sharper expiry — a project that later adds a backend, a mobile client or a second platform has no lawful path back, so its recorded decisions and applicable-system set freeze at day-one shape.

**Timeless form.** Give every step a stable slug that never changes: "### step: auth-wrapper" with "requires: [types, errors]" and "applies-when:" keyed to a References.md fact. Order becomes derived, not authored — a topological sort over requires edges and over the systems the project marked applicable — so a game or a CLI gets a short valid order; cross-references become "see step: idempotency", insertion touches one block, and no playbook contains "Step &lt;digits&gt;" so class B needs no special case. Convert the edges mechanically from today's order and diff the derived order against it once as proof. Phases become re-enterable with a recorded trigger (platform added, a revision requiring a new system, a dial past re-verification) logged in VERSION-LOG and scoped to the newly applicable systems, with the feature tree carrying the applicable set so declared shape versus built systems is auditable.

**Intent risk.** The numbers currently encode build-before-depend, and sloppy requires edges would make the order silently wrong, which is worse than a stale number — hence mechanical conversion and the one-time diff as proof. Re-entry must not become licence to re-scaffold over working code: the trigger is recorded and the phase is scoped to the newly applicable systems only.

**Verifier notes**

- **V1 held** on the ordering half by direct observation, and **trims the re-entry rhetoric:** "one-time" appears only in the factory planning doc, while `SCAFFOLD.md:23` already contemplates multiple playbook runs (fullstack = FRONTEND in one folder, BACKEND in another, each with its own References.md) and the VERSION-LOG scaffold template carries a typed "Shape:" and a "Systems deferred" field, so "no lawful path back" overstates; what is genuinely missing is a recorded trigger and a scoped re-run. **L** as stated, M if re-entry is descoped.
- **V2 held.** Searched `scaffolding/`, `development/` and `bootstrap/` for any re-entry path ("re-scaffold", "add a backend later", "second platform", "re-enter") and found none, so the no-path-back claim is verified by absence. **One overclaim noted:** a topological sort cannot produce a correct order for a product shape whose steps the framework has never authored; it only orders what exists. **L**
- **V3 held.** Reproduced the class-B collision verbatim. Confirms the order derivation needs no runtime the framework lacks (~20 nodes, with the one-time diff as proof) and that re-entry scoped to newly applicable systems is checkable in VERSION-LOG by `validate-maintain`. **L** (~1,000 lines of playbooks re-slugged plus cross-references plus the re-entry design).

---

## C19 — Backend conventions state one era's mechanism as the rule: presigned upload, a REST spine, an in-process middleware chain, a relational pool, two health paths

- **Kind:** structural-assumption · **Value:** 5 · **Sources:** backend
- **Adjusted effort:** reported L / L / L → **L (6-10 days)**, unanimous

**Evidence (agent-reported)**

- `dist/backend/CLAUDE.md:19` "Never proxy file uploads through the server. Use presigned URLs." with `B6-file-handling.md:5` making a vendor-coined mechanism the Principle and `:26` listing controlled pass-through as a violation.
- `dist/backend/conventions/B2-api-design.md:3` "This convention covers REST; for GraphQL see the subsection below" and `:50` "the REST rules above do NOT translate directly", against `backend/CLAUDE.md:18` "Never return raw arrays from API endpoints. Use the response envelope." — the absolute enforcer states the REST-only half, and typed RPC, event, realtime and in-process APIs get no carve-out.
- `dist/backend/conventions/B3-middleware.md:19` the one correct middleware order, `:20` "Never put authentication or authorization checks inside route handlers", `:29` a violation written against one framework's registration model.
- `dist/backend/conventions/B1-database.md:26` "Initialize one connection pool at application startup. Never create connections per request." (absolute at `backend/CLAUDE.md:10`) with `:27` ORM-generated SQL and `backend/Conventions.md:12` offering no path for a backend with no database; no backend convention carries an "Applies when" section.
- `dist/backend/conventions/B4-logging.md:30` "liveness at /health/live ... readiness at /health/ready ... Must respond under 1 second" while `:57` concedes "Research health-check patterns for the deployment platform"; `B4:25` "Never use console.log or print" enumerates two ecosystems.

**Why it expires.** Each rule's spine is a mechanism of the 2020s cloud: credential-delegated direct-to-object-storage upload, one HTTP transport's nouns and status map, a long-lived in-process chain that owns every stage, a long-lived process with one relational pool behind an ORM, and the two probes of one orchestrator. Deployments where these are impossible or wrong already exist — on-prem storage, resumable and content-addressed stores, regulated pipelines that must inspect bytes, per-invocation and edge runtimes, gateway-managed identity, document and key-value and embedded stores, worker-only backends — and an agent that cannot satisfy an absolute enforcer learns to ignore it.

**Timeless form.** Per convention: invariants in the rule, mechanism recorded or profiled. **B6** — the server authorizes before bytes move, the server owns the key, type and size are verified from the bytes, the application never holds whole-file bodies as a transfer step, all storage goes through one project-named interface; mechanism recorded (delegated credential, resumable protocol, inspected pass-through) with the reason, and magic-byte validation plus server-side key generation stay unconditional. **B2** — transport-neutral obligations (one contract source of truth, boundary validation that strips unknown fields, one uniform success and one failure shape each carrying a code and the correlation id, bounded collections with a documented continuation mechanism, caller-supplied idempotency keys on mutations, additive evolution with a contract-difference check) plus transport profiles that keep today's HTTP rules and status table verbatim alongside schema-graph, RPC, event and realtime peers, with "API transport" recorded. **B3** — ordering invariants (identity before permission before handler, validation before handler, an error boundary enclosing everything the project owns, a correlation id adopted or generated at the outermost owned hop and present in every log line, outbound call and error response) plus an ownership map naming exactly one owner per concern, no blanks allowed, per-object authorization non-delegable. **B1** — store-neutral invariants (no per-item fetch inside iteration, bounded result sets, atomicity as the store offers it, destructive change as add-then-backfill-then-remove with approval, client lifecycle decided once and sized to the runtime's concurrency model, access paths justified by observed queries and verified with the store's plan facility, logical deletion by default filter) plus store profiles keeping today's relational wording, with "Store class" and "Client lifecycle" recorded. **B4** — two questions answerable on demand (alive; can serve, dependencies reachable) cheaply and unauthenticated, with mechanism and actual caller recorded, readiness exercising dependencies and liveness never doing so. Diagnostics rules name the behaviour with the banned facility names recorded for the project's linter.

**Intent risk.** These absolutes exist because the default generated code is a buffering multipart handler, an unwrapped array, an auth check in a handler, a client constructed per request and a health endpoint that returns 200 while the database is dead. Force survives only if the forbidden thing is stated as the behaviour rather than the architecture (no whole-file buffering as a transfer step; no client opened in a request path), if the recorded mechanism demands a written reason so choosing the weaker path is visible in review, if the security rules are excluded from the recorded-choice move, and if each profile keeps today's concrete rules verbatim.

**Verifier notes**

- **V1 held.** Confirms every citation and that Step 62 generalized the vendor names inside these files (B6 alone had 12 class-A findings) while leaving the mechanisms as law, which the class system cannot see. Recommends a **minimum viable cut** — invariant-first rule lines plus one recorded-mechanism line per convention, profiles deferred — as an M-sized better first commit; full scope **L**.
- **V2 held.** Confirms no backend convention has an "Applies when" section, so a backend with no datastore has no lawful exit from B1. **Two cautions:** permitting "inspected pass-through" as a recorded mechanism carves out close to the behaviour the invariant forbids and needs an explicit streaming qualifier; and authoring RPC/event/realtime profiles invents guidance the framework has no scars for, where B2's existing GraphQL subsection shows the safer pattern (name the concerns plus a research instruction). **L**
- **V3 held.** Calls `B4:30` versus `B4:57` — the convention contradicting itself — the clearest proof the mechanism is not the invariant. **Note:** overlaps Step 62 follow-up 1 in method (core plus profiles) though not in content, so it should land inside that restructuring step rather than as a parallel structure; security absolutes stay outside the recorded-choice move. **L**

---

## C20 — Security and compliance rules fix one era's control list, one storage API and a closed list of regimes

- **Kind:** expirable-content · **Value:** 4 · **Sources:** conventions-a, conventions-b, backend, scaffolding, templates-scripts
- **Adjusted effort:** reported M / M / M → **M (3-5 days)**, unanimous

**Evidence (agent-reported)**

- `dist/conventions/23-app-security.md:23` "Configure security headers globally: Content-Security-Policy, Strict-Transport-Security, X-Frame-Options ..." — mandating a header already superseded by a CSP directive, with no Permissions-Policy or COOP; `:27` "All secrets come from environment variables or a secret vault"; `:54` the `postgres://` example.
- `dist/conventions/11-auth-security.md:21` "Never in client-accessible storage like localStorage. Use httpOnly cookies or secure server-side sessions." and `:39` — one platform's two APIs as the rule, leaving keychain, OS credential store, file-mode and workload identity with no floor.
- `dist/backend/conventions/B4-logging.md:12` (V2 reports `:11`) "If the project handles regulated data (HIPAA, SOC 2, PCI, GDPR right-to-audit, financial), an audit log is REQUIRED" with `:9` "2 weeks to 90 days" and `:10` "1-7 years depending on regime"; `scaffolding/RED-FLAGS.md:24` "SOC 2 CC7.2, HIPAA audit controls"; `templates/references-platform.md:122/125` state CMIA and HIPAA numbers flat, and no References template carries a compliance table.
- `dist/conventions/28-config-driven-content.md:21` "edit JSON, never edit view code", `:33` a three-layer resolver ending in a gitignored on-disk fallback, `:125` a live-looking studio email in the example.
- `dist/scaffolding/SCAFFOLD-MOBILE.md:53` "Permission models drift as OS versions release — a stale matrix is worse than no matrix" — the framework already knows the mechanism and applies it only to OS permissions.

**Why it expires.** A fixed enumeration of controls, a named storage API, a config format and a parenthesis of regime acronyms all read as the rule and as the test for whether the rule applies. Headers are superseded and extended; credential storage differs per platform and the cookie answer keeps moving; config can arrive from a control plane, a tenant row, a signed bundle or an editor; and a project subject to a regime absent from the list can satisfy the sentence literally and ship with no audit log, while "1-7 years" is specific enough to be used as the answer and wrong for the one regime that matters.

**Timeless form.** Threat and obligation first, control and citation recorded. **Headers:** state each threat property (cannot be framed by a hostile origin, transport pinned, no content-type sniffing, origins constrained for load and send) and record the current control set with a deprecation check at bootstrap, since a deprecated control passing a checklist is the failure here. **Secrets:** delivered by the platform's secret mechanism, never present in source, build output or client-reachable config, with mechanism and committed-placeholder convention recorded and the example scheme-neutral. **Credentials:** the strongest app-scoped store the platform provides — unreadable from code in the app's own execution context, app-scoped, session-bounded, fully cleared on logout — with the platform mechanism and its threat model recorded and the three-question test kept verbatim in Violations; browser mechanisms move to Research Notes. **#28:** edit configuration, never view code, with format, delivery path, precedence chain and accessor name recorded, the layers' property stated (deterministic precedence, each layer validated at the trust boundary), and examples on a reserved example domain (a validator rule). **Audit log:** the trigger becomes "any law, contract, certification or customer commitment requiring attributable evidence of who accessed or changed what, when and from where", with regimes, clauses and retention windows living in a dated References.md compliance table (regime, version, clause, requirement here, source, verified-on) that re-verification reads; a regime acronym in a convention or playbook must sit on a dated line or point at that table.

**Intent risk.** Named headers, "never localStorage", "edit JSON" and "HIPAA" are what make these rules instantly actionable and credible: stripping them risks a project shipping no headers while claiming compliance, self-certifying a credential store, or a founder in health care not sitting up. The concreteness moves to References.md rather than evaporating — the recorded control set is required before the security system can be marked built, the compliance table is a gate rather than an optional section, the obligation stays an unconditional REQUIRED, regime names stay as dated examples, and the staleness signal is advisory so nobody disables the check that protects them.

**Verifier notes**

- **V1 held** with two legs dropped: regimes themselves are durable and the text already hedges with "typically", so the closed-list reading is strained, and "edit JSON" in a shape-scoped convention is about as durable as any interchange format. The strong legs justify the work: a deprecating control enumeration a checklist will keep passing, a browser-only credential floor applied to native platforms, and undated compliance numbers in a shipped template. **M**
- **V2 held.** Confirms the credential property ("unreadable from code in the app's own execution context, app-scoped, session-bounded, cleared on logout") is a testable bright line that still excludes localStorage, so the floor survives moving browser mechanisms to Research Notes. **Two overstatements:** B4's regime parenthesis reads as examples after "regulated data", and both retention windows hedge, so the "ship with no audit log" harm is weaker than argued. **M**
- **V3 held.** Confirms `SCAFFOLD-MOBILE.md:53` proves the framework already owns the mechanism, and that a regime acronym needing a dated line or table pointer, plus a reserved example domain, are both grep classes in the existing validator. **Wording fix:** no bootstrap validator script exists, so "bootstrap gate" must be a handoff-check process step enforced at scaffold. **M**

---

## C21 — Applicability is optional prose, and a gate cannot say it did not apply

- **Kind:** missing-mechanism · **Value:** 4 · **Sources:** conventions-a, conventions-b, development
- **Adjusted effort:** reported L / L / M → **L (6-10 days)**, majority L

**Evidence (agent-reported)**

- `grep '## Applies when'` across the product returns only `dist/conventions/26-pulse-monitor.md`, `conventions/28-config-driven-content.md` and `templates/convention-template.md`.
- `dist/conventions/09-api.md:5` "If the project is backend-only (no client), this convention mostly does not apply — mark it as not needed in feature-tree.md" — the only escape hatch of its kind, in prose; while `conventions/21-routing.md:5/21`, `20-forms.md:20`, `04-components.md:10`, `13-performance.md:13` and `14-accessibility.md:14` state pointer-driven, URL-addressed, bundle-producing web rules as universal law.
- `dist/scripts/validate-framework.sh:91-104` group 4 requires Principle, Rules, Violations, Wrong vs Right and Research Notes — applicability is not among them (and the group calls `warn`, not `fail`).
- `dist/development/DEVELOP.md:72-82` the test baseline gives four minimum tests to "a new HTTP endpoint requiring authentication" and one line to everything else, so a command, tick, event handler or rendered view collapses the bar invisibly; `:139-143` the mandatory feature-doc section is "API Shape / Route(s)" with no alternative.
- `dist/development/RED-FLAGS.md:51` flag #8 presupposes a shared database throughout; `templates/feature-tree.md` instructs "Remove rows that don't apply", destroying the not-applicable/skipped distinction `09-api.md:5` asks for.

**Why it expires.** Half these conventions presume a product with a screen, a URL bar, a component tree and a build artifact — the shape the framework was born into — and the strongest correctness gate only binds when the feature has an "endpoint type". A text game, CLI, agent service, voice product or embedded target makes several conventions inert and several gates vacuous, and because only #09 says so in prose the framework cannot tell "not applicable" from "skipped", while agents learn to fill template sections with N/A.

**Timeless form.** Make applicability a declared, checked field with no files moved and no profiles introduced: every convention carries "## Applies when" with one line from a short closed precondition vocabulary kept beside the dial registry (has-human-ui, has-addressable-navigation, consumes-remote-api, produces-build-artifact, stores-user-credentials, ships-to-assistive-tech, has-persistent-store, multi-customer-template), genuinely universal ones declaring "Always"; `validate-framework` group 4 fails a convention without it; a feature-tree "not needed" mark must name the predicate that failed plus a one-line reason, and `validate-scaffold` accepts a missing system only when its predicate is false. Symmetrically, every gate declares an applicability predicate and reports NOT-APPLICABLE with the recorded reason rather than OK. Re-key the test baseline to entry points rather than HTTP verbs: each entry point gets four tests — rejects unauthorized invocation (or carries a written reason it has no authorization boundary), rejects malformed input, accepts valid input and asserts the result shape, covers one domain edge — with the project recording once how its entry-point kinds (request, command, event, scheduled run, user action, engine tick) map, and the feature doc's section renamed "Entry points and contract" with the route form as one filled example.

**Intent risk.** A predicate is an opt-out an agent under pressure will reach for ("no human UI" to dodge accessibility, "no build artifact" to dodge performance budgets), and "rejects malformed input" is vaguer than "400 on malformed input". Guards: the vocabulary stays short and objective, every false predicate is recorded in the project's own artifacts with a justification so skipping is visible, the no-authorization-boundary case must carry a written reason, and universal conventions declaring Always stop the section becoming a habit of exemption. This is the gate only and must not be mistaken for the recorded shape-agnostic-core proposal.

**Verifier notes**

- **V1 held.** Two findings strengthen it: `templates/feature-tree.md`'s "Remove rows that don't apply" actively destroys the distinction, and `validate-scaffold.sh:173-203` already implements exactly this predicate pattern for regulated data, so the mechanism is precedented rather than invented. Distinct from Step 62 follow-up 1 (no files moved, no profiles, plus an entry-point re-key that follow-up does not mention). **L**
- **V2 held.** **Two corrections:** group 4 calls `warn`, not `fail`, so today the five sections are only a warning and making applicability fail is a genuine increase in force; and the proposal's "References.md section Layout" does not exist (the section is "## Folder Structure" in four templates), so the wiring must name real sections. Net-strengthening: a background job/command/tick goes from two tests to four, and an invisible skip becomes a recorded one. Keep the status-code form as the filled HTTP example. **L**
- **V3 held.** Confirms precedent (`validate-scaffold.sh` already parses References.md and feature-tree.md and already accepts an explicit N/A) and that "kept beside the dial registry" names an artifact that does not yet exist, so this depends on C02. The expensive half is making every gate report NOT-APPLICABLE across four validators. **M**

---

## C22 — Repo-literal paths, code identifiers, manifest filenames and one file extension bind the docs to a single ecosystem

- **Kind:** expirable-content · **Value:** 4 · **Sources:** development, backend, conventions-a, templates-scripts, bootstrap
- **Adjusted effort:** reported L / L / L → **L (6-10 days)**, unanimous

**Evidence (agent-reported)**

- `dist/development/DEVELOP.md:32-37` "db (src/shared/db/) — will use (getDb() ...) / logger (src/shared/logger/) — (req.log ...)", `:68` "Never throw new Error(...) — use AppError subclasses from src/shared/errors/", `:86` "src/features/{name}/{name}.test.ts" — and a validator run on `DEVELOP.md` flags pytest, npm, Zod, SQLite and eslint and none of these.
- `dist/templates/feature-doc-template.md:21-26` the feature tree with `types.ts` and `index.ts`; `templates/references-frontend.md:155-159` "hooks/ # Feature logic" — `scripts/timeless-terms.txt:71` carries only `[A-Za-z0-9_-]*\.tsx`, so `.ts`, `.py` and `.go` filenames pass.
- `dist/conventions/02-git.md:31` "Creating variant files (utils-v2.ts, helper-new.ts)" and `:29` ".env files"; `conventions/03-architecture.md:13` "index file, __init__.py, or language equivalent" inside a universal section.
- `dist/backend/conventions/B4-logging.md:44` `logger.info({ correlationId, method: req.method, path: req.path }, 'Request received')` and `B6-file-handling.md:35` `if (file.mimetype === 'image/jpeg')` — the most imitated text in each convention is its least portable.
- `dist/bootstrap/EXISTING-PROJECT.md:174` "Cross-check against package.json and other authoritative sources" with the "or equivalent" list only at `:16` and itself closed; `bootstrap/ONBOARD.md:93` "ship @scope/* packages with placeholder tokens".
- V2 adds: `scripts/validate-develop.sh:22` aborts with "no src/features/ directory", and `:52-58`/`:136-146` hard-code `PrismaClient`, `getDb()`, `src/shared/db/`, `src/shared/errors/` and `throw new Error(` — a vendor name Step 62 removed from the docs survives inside the enforcing script, so a non-JS project's development gate cannot run at all.

**Why it expires.** Step 62 removed vendor names and left an ecosystem's directory and identifier vocabulary, which dates just as fast and is worse because the gates grep the same identifiers, so the accidental became enforced. A Go, Python, Rust or engine-based project reads rules whose operative token does not exist there and is left with two bad options: transliterate the example into a layout it does not use, or treat the step as inapplicable. The extension term proves the class was already recognised and covers one spelling.

**Timeless form.** Every concrete path and symbol in a managed doc is a dereference, not a literal: the inventory and rule text name roles (error type, data access, authorization check, request-scoped log sink, configuration, public entry point, type declarations, UI, logic) and resolve them through References.md's layout and Foundational Systems sections — the same registry the gates read (C17). Keep exactly one fully worked example per doc, clearly labelled as one project's filled registry inside a dated zone, and state Wrong-vs-Right in prose about shape ("the handler logs the whole incoming body; the right version logs the correlation id, method and path and no body") with any literal syntax fenced and language-labelled. References.md gains "Dependency manifest(s)" and "Package namespace convention", and the staleness audit cross-checks "the manifests recorded in References.md" rather than one filename. Broaden the extension term from `.tsx` to a source-extension family plus `.env*`, and add a class for repo-literal source prefixes (`src/`, `app/`, `lib/` followed by a slash) and bare code identifiers outside the dated zone.

**Intent risk.** This is where de-dating most easily destroys teeth: "use the project's declared error hierarchy" is ignorable in a way "never throw new Error(...)" is not, the filled inventory is what makes the first step performable, and the Wrong-vs-Right blocks persuade because they look like code an agent just wrote. Keep one labelled example, keep the obligation imperative (the role must resolve to a registry entry or the feature does not start), move the ban into the project's lint rule where a named facility is mechanically checked, and keep the greppable suffixes (-v2, -new, -old) in the violation text.

**Verifier notes**

- **V1 held.** Ran the validator on six in-scope cited files: "Pass: timeless check clean" while they carry `types.ts`, `index.ts`, `hooks/`, `src/features/{feature-name}/`, `utils-v2.ts`, `helper-new.ts` and the `logger.info` RIGHT line — so the gap is not merely the out-of-scope playbooks. Two weak spots: `03-architecture.md:13` already says "or language equivalent", and a detector for bare PascalCase names and camelCase-with-parens will fire heavily on ordinary prose. Effort raised to **L** (29 universal plus 7 backend conventions' Wrong-vs-Right prose, four References templates, feature-doc and feature-tree templates, playbooks, plus a new structural detector class needing false-positive tuning).
- **V2 held.** Reproduced the blind spot on `DEVELOP.md` (9 findings, none of them the paths or identifiers) and found the gate-side coupling that makes the registry dereference worthless until `validate-develop.sh` changes. Same "References.md section Layout" naming correction as C21. **L**
- **V3 held.** Same reproduction. Confirms the layer split (roles in the framework, resolution in References.md, the ban pushed into the project's lint rule) and flags the PascalCase detector as a false-positive machine, though Step 62 already set the precedent that class D leans that way. **L**

---

## C23 — Product docs narrate their own edit history, cite steps by ordinal, and carry years, dates, local paths and a private project name

- **Kind:** expirable-content · **Value:** 4 · **Sources:** development, bootstrap, conventions-b, backend, factory-design
- **Adjusted effort:** reported L / M / L → **L (6-10 days)**, majority L

**Evidence (agent-reported)**

- `dist/development/RED-FLAGS.md:21` "Old DEVELOP.md said run tests after every significant change" with `:23` citing "DEVELOP.md Step 4" where the baseline is now Step 5, and `:39-41` "DEVELOP.md Step 6 said commit as a save point ... Step 6 rewritten" where commits are Step 8; `MAINTAIN-RED-FLAGS.md:51` "MAINTAIN.md NO LONGER contains an inline audit script" and `:49` "This fired during the first Phase 4 agent run".
- `dist/bootstrap/hooks/README.md:60` "Claude Code exposes 21 lifecycle events as of 2026" and `bootstrap/hooks/post-task-verify.sh:6` "# Claude Code contract (2026):" — a bare year stamp in instructional text and in a script header no validator reads.
- `dist/backend/conventions/B1-database.md:21` "Name migrations descriptively with date prefix: 20260329_add_status_to_orders" — a literal calendar date stamping the convention with the month it was written.
- `dist/development/CREATING-FORKABLE-TEMPLATES.md:159-162` `~/Development4/makemyweb-dashboard/docs/...` and `~/Development4/templates/headless-wp-next/...`, with `:103` "The 10 patches the makemyweb dashboard's fork-template.sh accumulated" promoted to a normative checklist at `:115`; `conventions/27-design-foundation.md:45` "Factory authoring a framework Step" passes class B because it carries no digits.
- `planning/ARCHITECTURE.md:3` "## Timeless conventions (Step 62)" and `:7` "(Step 61)"; `planning/ROADMAP.md:17` "The checkboxes below retain their historical phase status"; factory `CLAUDE.md:69` "See Steps 44-49 for the pattern"; `planning/CHANGELOG.md` is ~1510 lines of date-and-ordinal headings with no subject-keyed current-state index.

**Why it expires.** Changelog prose in a product doc only means something to a reader who saw the previous release, and it re-expires at every rewrite; ordinal citations into sibling documents are already wrong three times over; a year or a calendar date dates the whole framework in one line; a home-relative path resolves on exactly one machine and a private project name is unreachable for every consumer. On the factory side the only index of why is an append-only numbered log, and the architecture document has begun to become a second one, so at 200 steps "what is the current rule about the entry point" is answerable only by reading the log in order — the failure References.md exists to prevent.

**Timeless form.** Rules and red flags are written present-tense in a fixed three-part form (the failure, the observable signal, the current defense), with the incident kept as a one-line dated "Observed:" note and the history left to the factory archive; cross-document references use named anchors, never ordinals. Add the classes the gate lacks: retrospective narration (used to say, no longer, rewritten, revised, was vague, fired during, old &lt;FILE&gt;), digitless factory references (framework Step, factory Step, a table row naming the factory or framework authoring as an actor), bare four-digit years and "as of &lt;year&gt;" and "current as of", date-shaped literals (YYYYMMDD and ISO dates) with placeholders passing, non-portable references (`~/`, `/Users/`, `/home/`, `C:\` and any named project that is not the framework), and bare step numbers in a table column headed Step — all failing outside a dated zone, with script comment blocks in scope. Concrete host facts become event roles, the migration example becomes a shape ("&lt;ordering-prefix&gt;_add_status_to_orders", never an opaque sequence number, with the project's ordering scheme recorded), and the patch war-story becomes a property checklist with one dated provenance line. Factory side: one current-state register keyed by invariant subject (current statement, evidence, revision verified, superseded pointers), `ARCHITECTURE.md` headings named for subjects rather than steps, step documents staying the immutable archive, and ROADMAP carrying one live queue with history archived.

**Intent risk.** The war-story framing is load-bearing motivation — "this actually happened" is why an agent believes a flag — and the ten concrete patches and the step log are how provenance survives. Keep the incident and the patch list as properties under a dated notice rather than deleting them, keep one dated provenance line instead of the machine path, and make the register store a pointer per statement so the append-only step documents remain the archive; two-sources-of-truth risk is answered by the revision stamp and the self-test.

**Verifier notes**

- **V1 held.** Ran the validator on `development/RED-FLAGS.md`: only line 35 flags (classes B and E); every retrospective-narration line, the wrong ordinals, the year stamp, the date literal and the machine paths pass. Confirms `B1-database.md` is fully clean while carrying `20260329_...`, and that `post-task-verify.sh:6` is in no scanned surface at all. Effort raised: six or seven new detector patterns, a rewrite of both red-flag documents, de-dating scripts and hooks prose, plus a factory-side current-state register and ARCHITECTURE/ROADMAP restructuring over a 1510-line log. **L**
- **V2 held.** Confirms that Step 62 follow-up 2 (remediate playbooks against existing classes) would catch none of this, so the detector classes are new work. Notes the narration being removed is already factually wrong three times over, so deleting it removes misinformation rather than force. Judges the product-side rewrite plus six classes a few days, the factory register over 62 step documents a week on its own. **M** for the product half.
- **V3 held.** Same verification. **Fit split:** the factory-register half is factory work and should be its own step; since the candidate scopes it as factory-side rather than pushing it into the product, it does not refute. **L** (three RED-FLAGS docs in three-part present tense, ordinals to anchors, six classes, plus a register distilled from 62 steps).

---

## C24 — The pulse monitor's durable contract carries one diagram DSL, a CDN library and one delivery mechanism, and nothing tests it

- **Kind:** missing-mechanism · **Value:** 4 · **Sources:** templates-scripts, scaffolding
- **Adjusted effort:** reported M / M / M → **M (3-5 days)**, unanimous

**Evidence (agent-reported)**

- `dist/templates/pulse-monitor-spec.md:3` "The UI is the expirable/replaceable layer; this doc is the contract" and `:63` "bump it whenever the shape changes, and update this section in the same change" — while `:65-106` documents generatedAt, dataContractVersion, project, techStack, foundationalSystems, features, architecture and drift and **no** `architectureDiagram`, and `scripts/pulse-inspect.sh:367` emits it while `templates/pulse-ui/app.js:148` consumes it, both sides still declaring v2 (`pulse-inspect.sh:357`, spec `:68`).
- `dist/scripts/pulse-inspect.sh:175` builds the field as `flowchart LR` — one tool's syntax inside the snapshot the spec promises will not change.
- `dist/templates/pulse-ui/index.html:57` loads mermaid@10 from `cdn.jsdelivr.net` while `scripts/timeless-terms.txt:276` lists Mermaid as a banned term — the name is expirable in prose and a hard runtime dependency in shipped code.
- `dist/scaffolding/SCAFFOLD-BACKEND.md:270` "Dev-only route (guard on NODE_ENV !== production): GET /dev/pulse serves templates/pulse-ui/index.html", `SCAFFOLD-FRONTEND.md:266` a third variant, `SCAFFOLD-MOBILE.md:108-111` "npx http-server archetype/templates/pulse-ui/ -p 4500" — mobile could not use the mechanism and got a variant with a port number, and the `archetype/` install literal appears in three files.
- `dist/scripts/validate-framework.sh:129-168` group 7 checks only the template columns the inspector parses, never the output shape it promises.

**Why it expires.** The durable half is a contract (an inspector emits a state document; a reader renders the sections) and what is written down is one delivery mechanism plus one renderer: a browser, an HTTP route, an environment-variable guard, a hardcoded install directory, a pinned CDN major and one library's DSL inside the data. Mobile is the proof, and any project without a dev HTTP server — a library, a CLI, a pipeline, the text game that triggered Step 62 — has no path at all. Meanwhile the contract is maintained by hand discipline across three files and has already drifted in the direction that matters.

**Timeless form.** Put the diagram in the contract as data (`architectureGraph: { nodes: [{id, kind, label}], edges: [{from, to}] }`) with the renderer an explicitly expirable project choice, a no-dependency fallback (node and edge list as a table or inline SVG) that always works, and any library loaded locally and optionally, never from a CDN in a distributed file. Make the surface a recorded adapter choice — served route, file opened directly, terminal renderer, editor panel — including a zero-infrastructure option so no shape has to skip, with the playbooks referring to "the project's pulse surface, per References.md" and never to a route, a port or a guard expression, and the install location resolved from References.md instead of the `archetype/` literal. Add a self-test group that runs the inspector against a tiny fixture and diffs emitted top-level and per-section keys against the JSON block parsed from the spec, failing on a key present in one and missing in the other and on mismatched contract versions. Fix the existing drift in the same change, or the new test lands red.

**Intent risk.** The concrete route, the script line and the library's output are why the pulse actually gets built and liked; an adapter abstraction invites "no suitable surface, skipped" and a hand-rolled renderer looks worse. Hence the always-working zero-infrastructure option, the library as an opt-in layer wired from the project's own manifest, and a key-shape-only test over a tiny fixture so legitimate extension stays cheap and the fixture never becomes a second contract.

**Verifier notes**

- **V1 held.** Confirms the drift line by line and that the spec's own sentence forbids exactly what happened. **One strand weaker than presented:** the spec already declares the UI the expirable layer, so mermaid in the browser is partly by design and the CDN objection is as much an availability concern as a timelessness one; the load-bearing defects are the DSL inside the data, the undocumented field at an unchanged version, and the playbook-level surface assumption. **M**
- **V2 held.** Reproduced the drift. **Required guard:** `SCAFFOLD-BACKEND.md:267` "Production builds MUST exclude the pulse UI" is a security obligation, so "never refer to a route, a port or a guard expression" must drop only the expression and keep "the pulse surface is never reachable in production" as a rule in the adapter contract. Corrects that the `archetype/templates/pulse-ui` literal is in two playbooks plus the spec. **M**
- **V3 held.** Confirms group 7 checks the inspector's input shape and never its output shape, that fit is framework-layer throughout, and that the fixture diff is key-shape only so it cannot become a second contract. **M**

---

## Proposed grouping into factory steps

Ten steps. Each line gives the candidates, the agent-reported day range (the S/M/L translation above, summed), and the reason the candidates belong together. Order respects the dependencies listed after the table; within that, higher-value candidates come first.

| # | Step | Candidates | Reported days |
| --- | --- | --- | --- |
| 1 | Framework self-checks: the framework proves its claims about itself | C01, C05, C06, C24 | 13-22 |
| 2 | Project record layer: dials, provenance, open gates | C02, C04, C12 | 9-15 |
| 3 | Declaration-driven gates: read what the project declared, three outcomes | C17, C09, C21 | 15-25 |
| 4 | Host and distribution portability | C07, C15, C08 | 12-20 |
| 5 | Authority, shared records, version-control neutrality | C10, C11 | 9-15 |
| 6 | Playbook step identity and applicability triggers | C18, C14 | 9-15 |
| 7 | Detection engine: invert scope and detection defaults | C13 | 6-10 |
| 8 | Numbers and premises | C03, C16 | 12-20 |
| 9 | Backend and security invariants | C19, C20 | 9-15 |
| 10 | Ecosystem literals and history narration | C22, C23 | 12-20 |

Total reported effort across all ten: roughly **106-177 days**, agent-reported sizes translated, nothing verified by the factory session.

**Step 1 — Framework self-checks (C01, C05, C06, C24).** All four are the same mechanism: a self-test that diffs what a shipped document claims against what the tree actually contains. C01 is the cheapest (S) and proves the harness while correcting numbers that are wrong today; C05 binds every Defense line to a check id; C06 resolves every reference, asserts index/enforcer membership and introduces the slug identity layer; C24 makes the pulse data contract executable by diffing emitted keys against the spec. Doing them together means one extractor, one differ and one id/slug vocabulary instead of four. C06's slug layer is a prerequisite for C18's step slugs, and C05's check ids are what C17 later reports three outcomes against. Required amendments: C06's resolver needs two namespaces (engine-relative vs project-relative paths), C01's catalog parity needs an explicit mapping rule rather than a raw set diff (append-only history, addon entries with no shipped file), and C24's existing v2 drift must be fixed in the same change or the new test lands red.

**Step 2 — Project record layer (C02, C04, C12).** All three write into the project's own artifacts and are read by new checks: C02 gives every delegated decision a declared id and a generated slot, C04 gives every researched line a verification stamp and a re-check path, C12 makes the unresolved-compliance-gate state machine-readable and stops a deferral reading as a negative answer. They belong together because C04's "which decisions does this changed convention own" and C12's gate lines both need C02's registry to be honest, and because all three extend the same References.md/VERSION-LOG shapes. The S-sized safety fix inside C12 (remove `skipped|deferred` from the not-regulated pattern) should land first and on its own.

**Step 3 — Declaration-driven gates (C17, C09, C21).** One pass over the same five validators. C17 makes each gate read the project's recorded layout and systems instead of grepping one era's vendor literals; C21 gives each gate and each convention an applicability predicate so NOT-APPLICABLE stops masquerading as OK; C09 makes each Verify line name an actor, a required capability and the evidence artifact it leaves behind. All three depend on Step 2's registry for their declared inputs, and all three share one outcome vocabulary (PASS / FAIL / NOT-APPLICABLE / NOT-VERIFIABLE, fail-closed). Splitting them would mean editing the same gate functions three times.

**Step 4 — Host and distribution portability (C07, C15, C08).** All three edit the installer and updater and all three remove a vendor or account literal from the critical path: C15 makes the canonical enforcer framework-owned with host-named generated copies and marker-based detection, C07 puts the hook event ABI behind a host adapter that declares whether it can block, C08 replaces the single hardcoded source with a manifest plus `--source`/`--revision`, a digest and a recorded promotion channel. C15's host registry is where C07's adapter declares its config path and blocking semantics, so they must land together. Non-negotiable ordering inside the step: the legacy-name load regression lands before the additions-file rename, and the self-replace path is digest-verified before the rename.

**Step 5 — Authority, shared records, version-control neutrality (C10, C11).** Both govern how a change is recorded and approved. C10 records authority, topology, escalation form and the unit of reviewable change, replaces the single ordered read with per-role entry sets, and makes shared indexes generated views with a one-entry-per-feature gate; C11 restates the save-point and gate rules as properties with the project's literals recorded so the prohibition stays greppable. They interact directly at "the unit of independently reviewable change" and at the shared-index fencing discipline. Mandatory clauses: the approver is never the author, a human stays the accountable authority of record, the unit carries a verification gate per unit (granularity dials, verification does not), and `02-git.md:20`'s secrets prohibition survives verbatim.

**Step 6 — Playbook step identity and applicability triggers (C18, C14).** C18 replaces position-as-identity with slugs plus requires edges and derived order, and makes phases re-enterable with a recorded trigger; C14 puts applies-when and provenance on the steps that were hoisted from one project's shape and moves shape-specific steps into router-loaded profiles. Same files, same router, and both use the predicate vocabulary defined in Step 3. This step is also the prerequisite for Step 7: the playbooks' own two-digit headers currently fail class B, which is what blocks bringing playbooks into the validator's scope mechanically.

**Step 7 — Detection engine (C13).** Invert both defaults: walk every tracked text file with per-file-type profiles and an exclusions registry carrying the allowlist's hygiene, add the unknown-proper-noun class checked against a vocabulary of what is legitimately nameable, and make class A case-insensitive only inside identifier contexts. It sits after Step 6 because the class-B collision must be gone first, and before Steps 8-10 because every later content pass needs the detector to see the surface. Two deductions recorded by the verifiers: the scope-inversion half substantially overlaps Step 62 follow-ups 2 and 3, so this step's novel content is the detection machinery; and the `planning/CONVENTIONS-CATALOG.md` restructure is factory housekeeping that should not gate the product-side work (a planning profile checking that each entry's dated block is marked is the part that belongs here).

**Step 8 — Numbers and premises (C03, C16).** The two halves of a rule's rationale. C03 widens the capacity-and-policy-threshold class and turns each surviving number into a registered dial; C16 separates the engineering invariant from the dated observation about tool and model behaviour and gives toolchain flags a probe and a retire-when. They touch the same paragraphs in the same ~36 conventions, so doing them separately means rewriting the same Principle twice. Both depend on Step 2 (a dial with no slot is a never) and Step 7 (the classes need the profile machinery). Sequencing note from the verifiers: widen the behavioural-word triggers only after the premise blocks exist, or the gate forces a mass rewrite and pushes authors toward vague prose. C03 carries a recorded dissent; the minimum defensible cut is the `backend/CLAUDE.md` vs `B5` contradiction plus the true capacity budgets into the registry, with standard-version pins left alone as a recorded Step 62 decision.

**Step 9 — Backend and security invariants (C19, C20).** Both restate domain rules as obligation plus recorded mechanism, and both need Step 2's recorded-mechanism slots and Step 3's gates to read them. C19 covers upload, API transport, middleware ownership, store class and health probes; C20 covers the security control set, credential floor, config delivery and the dated compliance table. Keeping them in one step keeps one rule for where a security absolute is exempt from the recorded-choice move. V1's minimum viable cut for C19 (invariant-first rule lines plus one recorded-mechanism line per convention, transport and store profiles deferred) is the better first commit, and V2's caution stands: do not author RPC/event/realtime profiles the framework has no scars for; name the concerns plus a research instruction, as B2's GraphQL subsection already does.

**Step 10 — Ecosystem literals and history narration (C22, C23).** Both are sweeps over the same prose: C22 replaces one ecosystem's paths and identifiers with role dereferences resolved through the project's registry, C23 rewrites retrospective narration into present-tense three-part form and removes years, date literals, machine paths and a private project name. They go last because C22's dereference is worthless until Step 3 has made the gates read the registry, and both need Step 7's detector classes to stay fixed. C23's factory-side current-state register is separable and should be its own factory step if the budget is tight.

### Candidates whose scope touches a recorded Step 62 follow-up

Flagged so nothing is funded twice: C13 (follow-ups 2 and 3 — scope extension and script comments; novel content is the detection machinery), C07 (follow-up 3 for the JSON/comment scan set; novel content is the host adapter), C14 and C19 (follow-up 1's method, core plus profiles, applied to playbook steps and backend conventions), C21 (explicitly not follow-up 1: no files move, no profiles). Follow-up 4 (updater no-op log entries) is untouched by all 24 candidates and remains open.

## Refuted list

**No candidate was refuted.** The sweep's refuted set is empty: all 24 survived three independent adversarial reviews each. What follows is the refutation that does exist, at verdict and strand level, one line of reason each.

**Candidate-level dissent (1 of 72 verdicts)**

- **C03, verifier 1 — REFUTED:** the cited lines are real but do not form the claimed class — "44x44 points" is density-independent and standard-derived, "WCAG 2.2 Level AA" is an open-standard pin Step 62 explicitly decided is not a tool choice, several cited numbers are already hedged dials, the Rules-lines-only scope gate is contradicted by the candidate's own Wrong-vs-Right and factory-side citations, and matching ~20 unit families with bare operators and spelled numerals trades one false negative for mass false positives on status codes, backoff ladders and agent-judgment heuristics; residue worth keeping is S-sized (one same-rule contradiction plus a few true capacity budgets into the dial registry).

**Strands the verifiers refuted inside candidates they otherwise held**

- **C01** — `README.md:74` "01-28.md" is cited as drift but is correct today (00 listed separately); it is an instance of the class, not a present error.
- **C01** — strict catalog id parity as a raw set diff is refused: it collides with append-only factory history and with catalog addons that have no shipped file, so it needs an explicit mapping rule.
- **C04** — "identical and undated in all 15 files of that surface" is wrong; the notice is in 36-38 files, which understates rather than invents the defect.
- **C09** — "accept no evidence" overstates: `SCAFFOLD.md:94-95` already requires "verify: &lt;command&gt; exit 0" per system, so this extends an existing field.
- **C09** — `SCAFFOLD-MOBILE.md:53`'s human review against store policy is durable compliance practice, not an era-bound belief, and must stay a permitted artifact.
- **C13** — the `planning/CONVENTIONS-CATALOG.md` full restructure is refused as factory housekeeping that contradicts an explicit recorded decision (`:7`, names are dated source notes) and must not gate product work.
- **C13** — `references-mobile.md:161/176` (HealthKit, Google Fit, Info.plist keys) is refused as evidence: those are target-platform API facts Step 62 deliberately excluded from the term list.
- **C14** — the red-flag-1 citation is glossed backwards (the flag is about agents skipping shape-tagged steps); the supporting sentence is its Defense, which supports the candidate.
- **C15** — "no mention of AGENTS.md anywhere in planning" is false; only `ARCHITECTURE.md` is stale.
- **C15** — "every generated copy matches the canonical source byte for byte" is unsatisfiable alongside an inserted banner; it must read body-identical below the banner.
- **C16** — widening class C to the words "pattern" and "intent" is refused: those fire on legitimate rule lines; actor nouns plus heuristic hedges are the defensible trigger set.
- **C18** — "one-time phases leave no lawful path back" overstates: the router already contemplates multiple playbook runs and the scaffold log carries a deferred-systems field; what is missing is a recorded trigger and a scoped re-run.
- **C18** — a topological sort cannot invent an order for a shape whose steps were never authored; it only orders what exists.
- **C20** — the closed-regime-list reading is strained (the parenthesis reads as examples and both retention windows hedge), and "edit JSON" is about as durable as any interchange format; both legs should be dropped.
- **C21, C22** — "References.md section Layout" does not exist (the section is "## Folder Structure"), so both proposals must name real sections.
- **C22** — a detector for bare PascalCase type names and camelCase-with-parens will fire on ordinary framework prose and needs heavy allowlist tuning or it drives authors toward the vagueness it exists to prevent.
- **C24** — the CDN-library strand is partly by design, since the spec already declares the UI the expirable layer; the load-bearing defects are the DSL inside the data, the undocumented field at an unchanged version, and the playbook surface assumption.
- **C24** — "never refer to a route, a port or a guard expression" must not drop `SCAFFOLD-BACKEND.md:267`'s production-exclusion obligation, which is a security rule, not an expression.
- **C19** — permitting "inspected pass-through" as a recorded upload mechanism carves out close to the behaviour the invariant forbids and needs an explicit streaming qualifier; and authoring RPC/event/realtime profiles invents guidance the framework has no scars for.

## Readers' coverage notes (verbatim)

**bootstrap**

> Read in full: the Step 62 planning document; all eight surface files (bootstrap/ONBOARD.md, EXISTING-PROJECT.md, LEARNING-PROJECTS.md, RED-FLAGS.md, REPOSITORIES.md, hooks/README.md, hooks/post-task-verify.sh, hooks/pre-destructive-warn.sh). Ran the validator read-only with explicit file arguments over the six surface Markdown files: 82 findings (class A 78, B 1, C 2, E 2; D and F clean) — consistent in kind with Step 62's recorded 73 for bootstrap/ plus the separately counted hooks README, so class A-F content already detectable in this surface is treated as covered by the recorded follow-up and is NOT proposed here. Read for context, not judged: validate-timeless.sh header and scope logic, timeless-allowlist.txt, grep probes of timeless-terms.txt (GitHub present and case-sensitive; k3s, minikube, Redpanda, linkerd, Confluent, EKS/GKE/AKS, Kafka, Istio, package.json absent), validate-framework.sh group list and groups 2-3, validate-scaffold.sh header plus its regulated-data group, development/FRESHNESS.md in full, root AGENTS.md in full, CLAUDE.md lines mentioning additions, templates/references-frontend.md first 60 lines. Verified facts: conventions/ holds 29 numbered files (00-28) against ONBOARD's two claims of 25 and 28; scripts/ has validate-scaffold, -develop, -maintain, -migration, -timeless, -framework but no validate-bootstrap; every path the surface cross-references exists. Skimmed only: the rest of validate-timeless.sh body and the remaining validator scripts. Not read, so not judged: scaffolding/ and development/ playbooks (out of surface), the 29 convention docs and backend conventions, the remaining References templates beyond the frontend head, inject.sh and update.sh internals (so the exact current shape of the recorded source pointer behind candidate 5 is inferred from REPOSITORIES.md and ONBOARD, not from the scripts), and any planning step other than 62 — if a later factory step already proposes one of these, I could not see it.

**scaffolding**

> Read in full: planning/STEP-62-TIMELESS-CONVENTIONS.md and all seven surface files (scaffolding/SCAFFOLD.md, _preamble.md, SCAFFOLD-FRONTEND.md, SCAFFOLD-BACKEND.md, SCAFFOLD-MOBILE.md, SCAFFOLD-PLATFORM.md, RED-FLAGS.md). Ran `bash scripts/validate-timeless.sh` with the seven files as explicit arguments (write-free): 101 violations, matching Step 62's recorded scaffolding/ count — 73 class A names, 28 class B step references, zero C/D/E/F. I deliberately did not propose anything whose only content is those 101 findings or extending the validator's default scope to playbooks; that is Step 62 follow-up 2. Candidate 1 is about the step-numbering scheme that makes that extension impossible without false positives, which is a different thing from scope.
>
> Read outside the surface, read-only, as corroboration for candidates 2, 6 and 8: scripts/validate-scaffold.sh in full (392 lines — groups 1, 2, 3, 4, 4b, 5, 6, 6b, 6c, 6d, 7; its source-root and env-validator candidate lists; its hardcoded migration-command patterns) and scripts/timeless-terms.txt only via the validator's output. I verified by grep that no rate-limit, idempotency, third-party-import, escape-hatch or stale-version check exists, and counted RED-FLAGS.md's numbered headers at 18 against the two prose statements of 13.
>
> Skimmed only, so judgments depending on them are not claimed: bootstrap/ and development/ playbooks (referenced repeatedly by my surface — I did not open bootstrap/RED-FLAGS.md, bootstrap/ONBOARD.md or development/DEVELOP.md, so I cannot say whether the handoff facts my surface expects from References.md are actually produced there, nor whether bootstrap's own step numbering has the same collision), templates/references-*.md and templates/pulse-monitor-spec.md (candidate 6's claim that these numbers have no slot rests on Step 62's record of which seven dials got slots, not on my own read of the templates), scripts/validate-framework.sh, and templates/pulse-ui/.
>
> Could not judge: whether downstream projects already depend on the current step numbers in their VERSION-LOG or feature-tree audit entries, which sets the real migration cost of candidate 1 (I read no downstream repository); whether any recorded founder steering already settled the platform-shape actor split in SCAFFOLD-PLATFORM.md:107 (Step 62 notes some steering lives in session memory outside both repositories, so candidate 4 may be relitigating a decision); and the true effort of candidate 8's probes, since I did not test whether a behavioural probe for class-downleveling is reliably writable in one command.

**development**

> Read in full: planning/STEP-62-TIMELESS-CONVENTIONS.md (all 163 lines, including the follow-up proposals, so I could avoid re-proposing the shape-agnostic core, the development/ name remediation, and the script-scanning note), and all eight surface files — /Users/d3r3nic/Development2/ai-dev-framework/dist/development/{DEVELOP.md, MAINTAIN.md, RED-FLAGS.md, MAINTAIN-RED-FLAGS.md, TASKS.md, FRESHNESS.md, CREATING-FORKABLE-TEMPLATES.md} and /Users/d3r3nic/Development2/ai-dev-framework/dist/META-BATTLE-TESTING.md. Read in full as supporting evidence: scripts/validate-develop.sh (all 158 lines — the vendor-named gates, the src|app|lib layout probe, the warn-vs-fail mismatch). Ran the validator read-only with explicit file arguments over all eight surface files: 51 findings (A 46, B 4, E 1), with "OK: no statistics attached to AI claims", "OK: no tool-capability numeric limits", "OK: every Research Notes section carries the dated notice" — that run is what let me separate "already detected, already a recorded follow-up" from "structurally undetectable", and every candidate here sits in the second group. Skimmed by targeted grep rather than read whole: scripts/validate-framework.sh (group 2's count check and its scope list), scripts/validate-maintain.sh (THRESHOLD_DAYS=180, audit-log threshold), templates/references-frontend.md and references-backend.md (dial slots present vs. absent — confirmed no test-isolation, audit-cadence, or debt-horizon slot). Verified factually: conventions/ holds 29 numbered files against META's "28 conventions"; development/RED-FLAGS.md holds 9 patterns against DEVELOP.md's "7"; DEVELOP.md's steps are 1-8 with tests at 5, docs at 7, commit at 8, against RED-FLAGS.md's citations of Step 4, Step 5, and Step 6; the two ~/Development4 paths cited in CREATING-FORKABLE-TEMPLATES.md do currently resolve on this machine, which is why I describe them as non-portable rather than broken. Could not judge: the bootstrap/ and scaffolding/ surfaces (other agents' scope, so I cannot say whether a mechanism I propose is better placed there — the delegation-integrity and count checks in particular are framework-wide and would need to run over all surfaces); whether the downstream game repository's References.md would accept the three new dial slots without conflict; and the TASKS.md/FRESHNESS.md pair, which I read in full and found already written timelessly (no tool names, no numbers, roles not vendors) — my only finding against them is that the playbooks' own artifacts cannot honour the fenced-write discipline those two files mandate. I did not modify anything in either repository and ran no script that writes.

**conventions-a** (dist/conventions/00-reusability.md through 14-accessibility.md, 15 files)

> Read in full: all 15 surface files (conventions/00-reusability.md through 14-accessibility.md), planning/STEP-62-TIMELESS-CONVENTIONS.md, scripts/timeless-terms.txt, scripts/timeless-allowlist.txt, and the header plus the entire awk scan body of scripts/validate-timeless.sh down to the class-A block (first ~200 lines; the output/summary tail after the scan I did not read, so I relied on observed behavior there rather than code). Read the header and groups 1-4 of scripts/validate-framework.sh (the reference-resolution and required-section checks my candidates extend); groups 5-10 only via the header comment list. Verification I ran: `bash scripts/validate-timeless.sh` with all 15 surface files as explicit arguments (writes nothing) — result "Pass: timeless check clean", all six classes OK, which is what lets me assert that 44x44, the 4px ramp, WCAG 2.2, 2MB/200KB, "80%+", localStorage/httpOnly/JavaScript, utils-v2.ts and __init__.py all pass today rather than merely guessing. Also grepped the surface for those strings and confirmed each line number quoted. Skimmed (not read whole): templates/references-frontend.md (the Foundational Systems and dial-slot region, lines ~40-145, plus greps for date/verified fields), templates/references-mobile.md and references-platform.md only via greps that showed they request a date while the frontend/backend templates do not, templates/convention-template.md and templates/hooks-spec.md via grep, bootstrap/ONBOARD.md via a heading grep to confirm "Step 3" resolves today, and a directory listing of backend/conventions/ to confirm B2-api-design.md exists. Not examined: the phase playbooks' bodies (out of surface and out of validator scope), backend conventions B1-B7 content, CLAUDE.md and Conventions.md content, scripts/validate-develop.sh / validate-maintain.sh / validate-scaffold.sh internals — so where a candidate proposes hooking a project-side check into those scripts, I verified only that the files exist, not that the hook point is convenient. What I could not judge: whether the founder's steering (kept in session memory outside both repositories, cited at STEP-62 line 148) has already settled questions like pinning an accessibility standard version, the 44x44 figure, or whether Git is deliberately a framework-level given rather than a project choice; three of my candidates would be overruled by such a directive. I also could not judge downstream cost — the game project's References.md and feature-tree.md are in another repository I did not open, so the migration burden of stable slugs or a mandatory Applies-when section on existing installs is estimated, not measured. Nothing in either repository was modified.

**conventions-b** (dist/conventions/15-build-ci.md through 28-config-driven-content.md, 14 files)

> Read in full: planning/STEP-62-TIMELESS-CONVENTIONS.md, and all fourteen surface files (conventions/15-build-ci.md, 16-documentation.md, 17-context.md, 18-verification.md, 19-steering.md, 20-forms.md, 21-routing.md, 22-design-system.md, 23-app-security.md, 24-authorization.md, 25-automated-enforcement.md, 26-pulse-monitor.md, 27-design-foundation.md, 28-config-driven-content.md). Also read for grounding, not judged as surface: scripts/timeless-terms.txt (header policy plus the database/framework/platform sections), scripts/timeless-allowlist.txt, dist/CLAUDE.md lines 1-60, templates/references-frontend.md lines 1-60 plus a grep for date/verify/dial fields, and a grep of the tree for `overrides` (update.sh preserves conventions/overrides/ on update, yet only #25 and #26 in this surface tell a reader the override path exists — a real but lower-value gap I left out of the twelve). Verified read-only: `bash scripts/validate-timeless.sh` with all fourteen surface files as explicit arguments reports clean (all six classes plus the justification check OK), which is what lets me claim the items above are undetected rather than merely unremediated; the `postgres://` finding in 23-app-security.md:54 is a demonstrated miss against the term at timeless-terms.txt:114. Nothing was modified in either repository. What I could not judge: whether the founder intends #20/#21/#22 to be read as web-only today (no applicability field exists to say), whether designtank.studio is a live third party (did not resolve it; the objection stands either way since #28 forbids hardcoded brand addresses), how the two conventions' "Project Overrides" blocks got there while twelve lack them, and the validator's exact regex behavior beyond the class descriptions in the step doc plus the clean run I observed — I read the terms and allowlist data files but not validate-timeless.sh itself, so my proposed class extensions are stated as intent rather than as patches to specific lines.

**backend** (dist/backend/CLAUDE.md, dist/backend/Conventions.md, dist/backend/conventions/B1-database.md … B7-caching.md)

> Read in full: all nine surface files (backend/CLAUDE.md 19 lines, backend/Conventions.md 29, B1 57, B2 73, B3 48, B4 61, B5 47, B6 46, B7 48); planning/STEP-62-TIMELESS-CONVENTIONS.md in full before judging; dist/templates/references-backend.md in full (the destination every backend Research Notes section delegates to). Skimmed: scripts/validate-timeless.sh header comment (classes A-F, scope, known limits) and scripts/timeless-allowlist.txt in full; scripts/timeless-terms.txt by targeted grep and line count (283 terms) rather than line by line, so a term I assumed absent could exist under different spelling — I verified absence for console/print/presigned/REST/health specifically. Verified by running, read-only with explicit file arguments: validate-timeless.sh on all nine surface files reports OK on all six classes and exits 0, so every candidate above is content the current validator passes. Also verified `grep -nE '\bB[1-7]\b' templates/references-backend.md` returns nothing. Not read, so not judged: the universal conventions (#0, #3, #7, #8, #9, #10, #11, #12, #23, #25 are all cross-referenced from this surface) — candidates 2, 7 and 8 propose mechanisms (dial registry, store profiles, dated-claim class) that may duplicate or collide with something already in the universal surface or in another surface agent's findings; the phase playbooks (out of validator scope, and they are where References.md actually gets filled, so the bootstrap-gate half of candidate 1 needs a playbook author to confirm feasibility); scripts/validate-framework.sh body, so my claim that a coherence self-test does not already exist rests on the Step 62 document listing group 10 as the only timeless-related group, not on reading the script. I modified nothing in either repository.

**templates-scripts**

> Read in full before judging: STEP-62-TIMELESS-CONVENTIONS.md (all 164 lines, including the follow-up list, so I could avoid re-proposing the shape-agnostic core, the playbook-scope extension, the "scripts carry comments" note, and the updater no-op log entries); and in dist/: AGENTS.md, CLAUDE.md, Conventions.md, README.md, update.sh, inject.sh, all 18 files under templates/ (including the two claude-settings JSONs and all three pulse-ui files), and all of scripts/ (validate-timeless.sh, timeless-terms.txt, timeless-allowlist.txt, validate-framework.sh, validate-scaffold.sh, validate-develop.sh, validate-maintain.sh, validate-migration.sh, pulse-inspect.sh, test-entrypoints.py). Ran read-only: validate-timeless.sh with explicit file arguments on four templates (clean — which is the evidence for the HealthKit/Google Fit/Info.plist/types.ts misses), plus greps for uncaught names, architectureDiagram, dataContractVersion, and verify/date markers. Wrote nothing in either repository and ran no script that writes. Not judged: the phase playbooks (bootstrap/, scaffolding/, development/), backend/ and conventions/ themselves, libraries/, and META-BATTLE-TESTING.md are outside my surface, so where a fix of mine touches them (a playbook naming a hook vendor, a convention that would trip an unknown-noun class) I could not measure the blast radius. I also could not verify behavior claims by execution: I did not run the phase validators against a real scaffolded project, so statements about validate-scaffold.sh 6b failing a compliant project and about pulse drift reporting every backend feature as missing (its scan roots are src/features and src/shared while references-backend.md:125 prescribes src/handlers/) are read from the code, not observed. Deliberately omitted as already recorded or too weak to spend a slot on: the fixed 16-system feature tree and the web-CRUD phase model (the recorded shape-agnostic proposal), the pulse-ui version comment (recorded follow-up 3), the duplicate VERSION-LOG entries (recorded follow-up 4), and sample dates like 2026-04-17 in technical-debt.md and references-platform.md, which read as format examples rather than claims.

**factory-design** (/Users/d3r3nic/Development2/ai-dev-framework/CLAUDE.md, planning/ARCHITECTURE.md, planning/CONVENTIONS-CATALOG.md, planning/STEP-61-TASK-RULEBOOK.md, planning/STEP-62-TIMELESS-CONVENTIONS.md, planning/ROADMAP.md)

> Read in full: /Users/d3r3nic/Development2/ai-dev-framework/CLAUDE.md (80 lines), planning/ARCHITECTURE.md (272), planning/CONVENTIONS-CATALOG.md (515), planning/STEP-61-TASK-RULEBOOK.md (29), planning/STEP-62-TIMELESS-CONVENTIONS.md (164), planning/ROADMAP.md (97). Read as supporting evidence, partially and read-only: dist/scripts/validate-timeless.sh (scope loop at 77-87, the class-D regex at 161, the awk precondition), dist/scripts/timeless-terms.txt (283 lines; header policy and a grep for host/model/hook-event terms: Copilot and ChatGPT present, no hook event names, no model tiers), dist/CLAUDE.md (Enforcement section in full: 26 bullets), dist/templates/hooks-spec.md (lines 20-22), dist/templates/references-frontend.md (dial slot lines), dist/update.sh and dist/inject.sh (greps for the forge URL, the preserve list, the revision hash), dist/bootstrap/ONBOARD.md (clone lines only), plus directory listings of dist/, dist/conventions (29 files, #00-#28) and dist/backend/conventions (7). Ran dist/scripts/validate-timeless.sh with explicit file arguments over the five surface files that live outside its scope; it writes nothing, and its output is quoted as evidence for the catalog findings and for the class-D miss on "<300 lines". Did not read: planning/PLAN.md and planning/CHANGELOG.md (not in my surface), so claims about the step archive rest on what CLAUDE.md, ROADMAP.md and the two step documents say about it; the phase playbooks under dist/bootstrap, dist/scaffolding and dist/development beyond the ONBOARD clone lines, so the phase-model candidate is judged from ARCHITECTURE.md's stated model rather than from playbook text; dist/Conventions.md, dist/AGENTS.md and dist/META-BATTLE-TESTING.md contents (their existence only). Modified nothing in either repository. Could not judge: whether any unread factory document already proposes the dial registry, the host-adapter registry or the parity self-test; the two recorded Step 62 follow-ups (playbook scope, shape-agnostic conventions core) are deliberately excluded, and the scope-inversion and phase-engine candidates are written to name the part those follow-ups leave standing.

## Coverage gaps the factory should close before funding any step

Drawn from the readers' own notes above, stated as gaps in this sweep, not as findings:

- No downstream project repository was opened by any reader, so every migration-cost estimate (stable slugs, the additions-file rename, a mandatory applicability section, new dial slots) is estimated, not measured.
- `planning/PLAN.md` and `planning/CHANGELOG.md` were read by no reader, so no candidate can claim that an unread factory step does not already propose the same mechanism.
- Founder steering kept outside both repositories (recorded at `STEP-62:148`) may already settle C03's standard-version pin, C09's platform actor split, and whether the VCS in C11 is deliberately a framework-level given; three candidates would be narrowed or overruled by such a directive.
- No reader ran the phase validators against a real scaffolded project, so the behavioural claims in C17 and C24 about what a compliant project would see are read from code, not observed.

## Completeness critic (verbatim)

### Gaps

- research/ (5 files, 2,188 lines: research/industry/full-conventions-research.md, audit-findings.md, backend-conventions-research.md, ai-first-conventions-research.md, research/existing-rulesets/ANALYSIS.md) was read by no reader and is named nowhere in the report, yet it is the un-remediated source of exactly the content Step 62 deleted. Verified by grep: '2.74x higher vulnerability rates' at research/industry/audit-findings.md:90; '45% of AI code has security vulns' at backend-conventions-research.md:26 and :70; '90-100% of AI-generated code' at ai-first-conventions-research.md:223 and :408; '80-90%' at :226, :227, :561; 'Files under 300 lines' at full-conventions-research.md:1169. C13's novel argument (the mould is dirtier than the product, so the next author re-introduces what Step 62 deleted) applies with more force to research/ than to CONVENTIONS-CATALOG.md, because the catalog's 'Includes:' lines are themselves derived from research/ (catalog:4 'Sources: [A]...[B] Traditional research (500+) | [C] AI-first research'). research/ also already demonstrates the provenance form C04 and C16 ask for (ai-first-conventions-research.md:223 dates its source: 'OX Security "Army of Juniors" report (October 2025)'), so it is evidence for those candidates as well as an unexamined surface.
- planning/BACKEND-AUDIT.md was read by no reader and is not listed in the report's coverage-gap section. It is the backend conventions' mould, the exact analogue of CONVENTIONS-CATALOG.md for B1-B7 (head reads 'Audit of 37 backend-specific conventions against the 23 universal conventions' with a table assigning content to B2/B6 etc.). C13's planning-profile argument and C19's whole backend restructuring therefore both have an unread upstream: if C19 rewrites B1-B7 as invariants-plus-profiles, the document that prescribes what belongs in each B-file is not in scope to follow.
- planning/STEP-52-DEPLOY-AUTOMATION.md was read by no reader and is not listed as a gap. It is the obvious upstream of C14's hoisted deployment lore and of C08's distribution assumptions (its trigger line reads 'Edgar's first manual deploy (2026-04-22) surfaced the full sequence, IAM bindings, ignore-file requirements, env discipline, and ingress/DNS work required per customer' and its next section opens 'Architecture bifurcates by billing mode ... choose by who pays'). C14 proposes a mandatory 'provenance: naming the shape it came from and the date observed' line for exactly the SCAFFOLD-FRONTEND.md Step 11a bullets that descend from this document, but nobody read it, so the provenance the candidate wants written cannot be sourced and the candidate cannot tell whether the lore is already recorded correctly one layer up.
- The factory root README.md (34 lines) was read by no reader, is cited by no candidate, and is stale in precisely C01's drift class and C23's narration class. Verified: its structure block lists only ARCHITECTURE.md and ROADMAP.md under planning/ (there are 9 files), only 2 of the 5 research/ files, and a 'dist/References.md # Layer 3: Template' entry that does not exist in dist/; its Status section reads 'Phase 1: Foundation (in progress)' after 62 completed steps. C01 proposes generalizing the count check to 'any managed file' and C06 proposes resolving every path token, but no reader established whether the factory README is in either candidate's managed set.
- No automated trigger for any gate exists anywhere. Verified: no .github/ in the factory or in dist/, and no .yml or .yaml file in either tree. validate-framework.sh group 10, validate-timeless.sh, test-entrypoints.py and all five phase validators run only when an agent or human remembers to run them. Six candidates add new self-tests (C01 count/parity harness, C05 check-id differ, C06 coherence resolver, C13 detection engine, C15 generated-copy assertion, C24 contract-key diff) and not one examines what would run them. This is the framework's own recorded silent-failure pattern applied to the sweep's remedies: dist/development/MAINTAIN-RED-FLAGS.md:38 reads 'The shipped hooks ... do NOT check doc freshness. The defense was name-only.'
- The execution environment of the framework's own tooling is an unexamined era-bound class. Verified: dist/scripts/validate-maintain.sh:153-155 uses BSD-only 'date -j -f "%Y-%m-%d"' (guarded, so on GNU coreutils the staleness loop silently finds nothing); scripts/test-entrypoints.py requires python3; update.sh requires git, mktemp and a clone; every gate is bash+awk+grep and Step 62 records awk interval support as a checked precondition. C07 abstracts the host event ABI, C08 the transport, C17 the vendor literals inside gate patterns; nobody asks whether the gates can run at all on a non-POSIX userland, without git, or on GNU date. A validator that silently passes because its date parser is absent is the same vacuous-OK defect C17 is built around.
- Natural language and locale is an unexamined class. The framework is monolingual in both prose and detection: class C's trigger list, class D's limit-word alternation (validate-timeless.sh:160-163, 'Under|Below|Beyond|Over|...'), and C13's proposed unknown-proper-noun class and C22's bare-identifier class are all English regexes; conventions assume English identifiers and LTR reading order. Related and also unexamined: CONVENTIONS-CATALOG.md:482 and :488 record A1 INTERNATIONALIZATION and A2 OBSERVABILITY as optional addons that were never shipped as convention files. The report names A1/A2 only as an obstacle to C01's id parity, never as a shape/era gap in its own right.
- dist/libraries/ is cited by C13 as an unscanned default-scope surface, but it holds nothing to scan: 'find dist/libraries -type f' returns nothing (only an empty dist/libraries/react/ directory) and 'git ls-files dist/libraries' is empty, so it is untracked and C13's own proposal ('walk every tracked text file') would not reach it. That is one of four surfaces C13 lists to justify inverting the scope default; the hollow item should be replaced by a real one before the step is funded.
- dist/Conventions.md was examined only as part of the templates-scripts surface, treated there as an index/enforcer. But C06 asserts membership checks against 'its index and quick-lookup table', C21 would add an applicability predicate per convention, and C11 would retitle #2; no reader quoted the quick-lookup table or established its shape, so the routing-layer half of C06's claim ('the routing layer is hand-maintained and already disagrees with the conventions it cites') rests on no quoted evidence from the file that holds the routing.
- planning/PLAN.md and planning/CHANGELOG.md (1,510 lines) are acknowledged as unread, but the consequence is understated rather than theoretical. Verified by grep in CHANGELOG.md: :691 already records 'new Group 7 "Templates <-> pulse-inspect parse contract"' and :659 'new Group 8 "No project artifacts inside the framework folder"' - self-tests of exactly C01's and C06's kind, designed in earlier steps. So the risk that a candidate restates an existing factory design is demonstrated, not hypothetical, and it cannot be cleared without reading the log.
- The published product carries no license or redistribution terms (no LICENSE file in dist/; verified). C08 redesigns the whole distribution channel - manifest, --source, --revision, digest, recorded promotion channel - without touching what authorizes a consumer to hold or redistribute the managed files that update.sh rm -rf's and re-copies into their repository. Lower value than the other gaps and arguably not era-bound, but it is the one distribution-surface question the sweep did not ask.

### Claims the critic could not verify or found contradicted

- C03's sole candidate-level refutation rests on a misreading of the line it cites, and I opened it. STEP-62-TIMELESS-CONVENTIONS.md:33 is about the TERM LIST only: 'Terms live in scripts/timeless-terms.txt as case-sensitive whole-word extended regexes with a category ... Languages, open standards (HTTP, JSON, OWASP, WCAG, ARIA, OpenAPI, GraphQL), regulations, and the platforms a product ships to (iOS, Android, the web) are not terms: they are not tool choices.' It decides that WCAG is not a vendor name for class A. It says nothing about whether the version suffix in 'WCAG 2.2 Level AA' is era-bound content. V1's claim that STEP-62:33 'explicitly holds' a standard-version pin is not a tool choice, and therefore that 'stripping standard-version pins reverses a recorded decision', is not supported by the cited line - and the report propagates it twice more, into the refuted-strand list and into the Step 8 funding instruction 'standard-version pins left alone as a recorded Step 62 decision'. The only candidate-level REFUTED verdict in 72 should be re-adjudicated.
- The report contradicts itself on that same line. C11/V3 reads STEP-62:33 narrowly and correctly - 'STEP-62:33 exempts languages, standards and ship targets but never considers the VCS or a CLI flag, so this gap is unexamined rather than decided' - while C03/V1 reads it as a settled decision about version pins. Both readings cannot stand; the narrow one matches the text.
- C06's enforcer-contradiction evidence does not hold when the files are opened. backend/CLAUDE.md:18 'Never return raw arrays from API endpoints. Use the response envelope.' is cited as contradicting B2-api-design.md:53, which actually reads 'GraphQL's own response format is the envelope. The REST { data, meta, errors } rule maps to GraphQL's built-in data and errors fields. Do not layer an additional envelope inside.' That satisfies the absolute, it does not contradict it - and B2 itself carries the same absolute at :26 ('Never return raw arrays at the top level'). V1 reports having 'confirmed the backend/CLAUDE.md:18 vs B2:53 contradiction'; on the text there is no contradiction. C19 reuses the same item as its B2 evidence.
- C19 overstates the absence of applicability prose in the backend set. Verified B2:3 'This convention covers REST; for GraphQL see the "GraphQL" subsection below - the core principles (consistent contract, validation at boundary, versioning discipline) still apply, but the mechanics differ significantly' and B2:50 'If the server uses GraphQL instead of REST, the REST rules above do NOT translate directly. Research current GraphQL server patterns ...'. So 'typed RPC, event, realtime and in-process APIs get no carve-out' is true only for transports other than GraphQL, and V2's 'no backend convention has an "Applies when" section, so a backend with no datastore has no lawful exit from B1' is verified for B1 but is generalized in the candidate text into an absence of all applicability prose, which B2 refutes.
- Every reported count of the dated-notice file set is wrong. Verified: grep -rl 'Dated notes:' across dist returns 39 files. The candidate says 15, V1 says 38, V2 says 38 (29 universal + 7 backend + 2 templates), V3 says '36 conventions plus templates'. The direction of the defect is unaffected, but three independent verifications of the same grep produced three wrong numbers, which is a signal about the sweep's count discipline generally (see the next item).
- C01's own headline undercounts its own class. Verified six wrong numbers on the product side: bootstrap/ONBOARD.md:59 '25 convention docs' and :366 'all 28 convention docs' (against 29 files); scaffolding/_preamble.md:44 '13 known silent-failure patterns' and scaffolding/SCAFFOLD.md:35 '13 silent-failure patterns' (against 18 '^## [0-9]' headers in scaffolding/RED-FLAGS.md); development/DEVELOP.md:20 '7 Phase 3 silent-failure patterns' (against 9); META-BATTLE-TESTING.md:14 '28 conventions'. Plus planning/ARCHITECTURE.md:50 '19 enforcement rules', :59 'all 25 conventions', :65 '(23 files)' and CONVENTIONS-CATALOG.md:3 '= 25 total'. V1's 'six, not five' is right; the candidate title is wrong.
- Five mutually unreconciled dated-zone grammars are proposed and funded in four different steps, and nothing in the document reconciles them. C04 wants 'As of: YYYY-MM' added to the existing notice plus a per-bullet tail '| verified <date> | source <...> | review <...>'; C16 wants a new required block '## Observed failure mode' carrying the notice plus its own As-of, or a two-line Symptom/Check; C14 wants a 'provenance:' line naming shape and date observed; C23 wants a one-line dated 'Observed:' note; C20 wants a dated compliance table (regime, version, clause, requirement, source, verified-on); C13 wants per-file-type profiles in which 'Research Notes and dated blocks stay fully exempt'. Class F today requires exactly one notice form ('every Research Notes section carries the dated notice', confirmed by running the validator). Whichever of steps 2, 6, 8, 9, 10 lands first sets a shape the others contradict.
- Three candidates push concrete era content back into the product without reconciling that against the one audit item Step 62 explicitly DECLINED. STEP-62:148: 'Declined: restoring vendor names inside Research Notes. The founder's steering for this framework, kept in the founder's session memory outside both repositories, is that Research Notes name categories of tooling, not tools'. C19 requires that 'each profile keeps today's concrete rules verbatim' including the HTTP status table; C14 requires that 'the deployment lore keeps its full text in the profile rather than being summarised away' (the lore being vendor ingress, NEG/service primitives, ACME proxy behaviour and vendor IAM epochs, verified at SCAFFOLD-FRONTEND.md:246-256); C16 requires keeping the observation 'verbatim and adjacent'. No candidate flags the collision. This is the one place the sweep may be re-litigating a founder decision, which is exactly what the brief asked to avoid.
- C18's remedy is broader than its evidence and partly reverses a Step 62 decision. Step 62's class-B definition states 'playbook step references are single-digit inside a named document and stay legal'. The collision I reproduced is only with two-digit headers: SCAFFOLD-FRONTEND.md's headers are Step 0, 1, 1b, 2-9 (legal) then Step 10 at :158, 11 at :182, 10b :196, 10c :210, 10d :221, 11a :225, 11b :259, 12 :273. So the sequencing argument (two-digit headers block pointing the validator at playbooks) holds, but 'no playbook contains "Step <digits>"' goes past the defect and past the recorded decision.
- C13 and C22 independently propose overlapping detector classes and are funded in different steps, with neither acknowledging the other. C13: an unknown-proper-noun class over CamelCase, dotted identifiers and SCREAMING_SNAKE, plus case-insensitive class A inside identifier contexts. C22: a class for repo-literal source prefixes (src/, app/, lib/) and bare code identifiers outside the dated zone, plus broadening the extension term. Both verifier sets independently flag the same false-positive risk on PascalCase and camelCase-with-parens. The grouping puts C13 in step 7 and C22 in step 10 as if they were separate machinery.
- C15 and C07 name different homes for the same host registry. C15: 'a hosts/<host>/ adapter that declares entry filename, whether the host auto-loads it, the global-preferences path, the hook binding and blocking semantics'. C07: 'bootstrap/hooks/adapters/<host>.sh is the only place host strings live ... and declares config path, format and can-block'. The step-4 note asserts 'C15's host registry is where C07's adapter declares its config path', but as written the two candidates specify two registries at two paths with overlapping fields.
- C05 and C17/C21 disagree about the gates' output vocabulary, in the order the steps are funded. C05 (step 1) fixes the emit format as stable ids in an OK line ('OK [scaffold.audit-log-separate]: ...') and builds a differ against it; C17 and C21 (step 3) replace the two-outcome model with PASS / FAIL / NOT-APPLICABLE / NOT-VERIFIABLE, fail-closed. Step 1 therefore standardizes and tests an output shape step 3 must re-cut, and the report's rationale for the order ('C05's check ids are what C17 later reports three outcomes against') does not address that the verb-severity assertion C05 adds is written against fail()/warn(), which step 3 changes.
- The 'References.md section Layout' naming error is corrected for C21 and C22 but left standing in C17, which has it too. I verified the actual '## ' sections of templates/references-backend.md: Project, Tech Stack, Commands, Foundational Systems, Folder Structure, Existing Patterns to Study, Critical Lessons, Convention Overrides, Project-Specific Documentation. C17 proposes 'A References.md "Layout" section records source roots, shared module root, wrapper boundary ...' and is not listed among the corrections, so the one candidate whose entire mechanism is 'the gate reads the declared section' names a section that does not exist.
- C17's three-outcome design does not cover the failure mode its own evidence file already has. Verified validate-develop.sh:21-24 probes four directory names and :22-24 exits 1 with 'Error: no src/features/ directory. Run from a project root with a features/ tree.' before any group runs. So for that validator a missing or differently-shaped layout is not a silent OK, it is a hard abort - a fourth outcome ('the gate cannot start') that neither C17 nor C22/V2 (which cites the same line as coupling) states, and that fail-closed NOT-VERIFIABLE does not describe.
- C02's strongest-sounding check would fire on a line the framework deliberately made optional. Verified conventions/17-context.md:16: 'Compact context proactively during long sessions. Don't wait until the context window is full; the tool's window size sets the cadence, and the project records a cadence in References.md if it sets one.' C02's proposed check - prose saying 'record/document it in References.md' or 'the project sets' must resolve to a declared id with a slot in every applicable template, and a project-side gate fails on an unfilled slot - contradicts 'if it sets one', so either the rule must be rewritten or the check needs an optional-dial kind the candidate does not define.
- C20's security-header leg is reported without the hedge the line carries. Verified conventions/23-app-security.md:23 ends 'Research the framework's recommended security middleware', so the enumeration is already paired with a research instruction rather than presented as a closed list. V1 already dropped two of C20's four legs (closed regime list, 'edit JSON'); this weakens a third, leaving the undated compliance numbers in templates/references-platform.md and the browser-only credential floor as the load-bearing evidence.
- Two small citation errors in candidates that propose ordinal detectors, which matters because the detector would be built from these patterns. C23 cites factory CLAUDE.md:69 for 'See Steps 44-49 for the pattern'; the sentence is at line 68 and uses an en dash ('Steps 44-49' rendered with U+2013), which a naive 'Step N' range pattern would miss. C07 cites planning/ARCHITECTURE.md:170-171 for two PostToolUse entries; they are at :169 and :170.
- Two claims that are true as written but whose force the report misstates. (a) C05's 'RED-FLAGS.md:93 "can grep for obviously-stale version strings"' - verified, and the line continues 'but is best-effort here - the real defense is the research-at-scaffold rule', so it is a hedge, not a false claim of coverage; V2 says this and the candidate's evidence list does not. (b) C12's write-ordering claim - verified ONBOARD.md:144 does say 'Record in VERSION-LOG.md as open pre-production gate', so the defect is format and consumer plus the inversion at validate-scaffold.sh:179 (negation filter containing 'skipped|deferred' applied before the affirmative regime match at :183, then the pass at :203), not that the gate is written last. Both corrections are in the verifier notes but the candidate headlines were not amended.
